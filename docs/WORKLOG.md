# 完工日志（只追加，不修改历史行）

2026-09-18 | claude | 重新部署 Edge Function `attribution-sync-creators`、`feishu-read-connection-stats`、`attribution-feishu`（main 当前版本，无代码改动） | supabase/functions/{attribution-sync-creators,feishu-read-connection-stats,attribution-feishu}/index.ts, src/lib/version.ts, docs/WORKLOG.md

格式：`日期 | 工具 | 改了什么 | 涉及文件`

2026-09-14 | lovable | 执行 migration 20260914120000_finalize_chunked.sql，新增 attribution_clear_upload_agg / attribution_build_upload_agg_part / attribution_finalize_mark 三个 service_role 分片归并函数；重新部署 attribution-upload Edge Function 使大文件分片 finalize 生效 | supabase/migrations/20260914120000_finalize_chunked.sql, supabase/functions/attribution-upload/index.ts, src/lib/version.ts, docs/WORKLOG.md

2026-09-14 | lovable | 重新部署 Edge Function attribution-upload（代码已含最新共享逻辑），版本号改为 0914.052 | supabase/functions/attribution-upload/index.ts, src/lib/version.ts, docs/WORKLOG.md

2026-09-10 | lovable | 修复 attribution-upload 的 10 万行 finalize 超时：新增仅 service_role 可调用的 attribution_finalize_upload RPC，在数据库内原子完成归并、汇率校验、金额汇总和 READY 状态更新，并用 advisory lock 防止同批次并发重算；Edge Function 删除归并后读回全部结果及即时归因预览，前端改为上传完成后刷新月度快照；版本号改为 0910.036 | supabase/migrations/20260910162142_eaf75eb7-658d-46d8-b77f-48be76bd0919.sql, supabase/functions/attribution-upload/index.ts, src/lib/{attributionApi,uploadQueue,version}.ts, src/components/attribution/UploadView.tsx, docs/{ARCHITECTURE,PLAN,WORKLOG}.md

2026-09-09 | lovable | 二次修复 attribution-upload 查询超时：unmatched_trend 将 12 个月一次性数据库聚合拆成逐月聚合并在函数内合并，避免大月份和并发查询撞 statement timeout；无需新增迁移；版本号改为 0909.022 | supabase/functions/attribution-upload/index.ts, src/lib/version.ts, docs/{ARCHITECTURE,PLAN,WORKLOG}.md
2026-09-09 | lovable | 修复 attribution-upload 查询超时：unmatched_trend 从逐页拉取近 12 个月约 58 万条明细改为数据库内按国家×达人×月份聚合；新增仅 service_role 可调用且带 120 秒预算的 attribution_unmatched_trend_json RPC，以单个 JSON 返回完整结果避免默认 1000 行截断，并增加 UNMATCHED 覆盖索引；迁移已执行并重新部署 attribution-upload；版本号改为 0909.021 | supabase/functions/attribution-upload/index.ts, supabase/migrations/20260909172925_2e561aca-50af-4178-ae25-2d90fcb309d0.sql, supabase/migrations/20260909173053_f73d6206-d1d9-40d3-91bb-9e68cd8aee7f.sql, supabase/migrations/20260909173131_0e9146cf-8adf-466f-bac2-dc88f16a8c87.sql, supabase/migrations/20260909173353_90b1729c-06a6-4ef1-9016-ad0bf7142f92.sql, src/lib/version.ts, docs/{ARCHITECTURE,PLAN,WORKLOG}.md

2026-06-10 | claude | 建立协同文档体系（AGENTS.md / CLAUDE.md / docs/） | AGENTS.md, CLAUDE.md, docs/*
2026-06-10 | lovable | 校准 PLAN：3 条历史任务标完成（默认 tab/日期、GMV Max cron job、索引） | docs/PLAN.md
2026-06-10 | lovable | 新增每日 08:00 自动授权 cron：authorize-cron 路由 + authorize_cron_state 表 + 飞书机器人通知；3 个 Edge Function 加 x-cron-key bypass | src/routes/api/public/hooks/authorize-cron.ts, supabase/migrations/*_authorize_cron_state.sql, supabase/functions/{feishu-read,authorize-batch,feishu-writeback}/index.ts, docs/ARCHITECTURE.md
2026-06-10 | lovable | feishu-read 改为固定列布局（G=VID / H=授权码 / I=SKU，删除 legacy G 列授权码分支）；执行授权页顶部加可折叠「📖 使用说明」（3 个 tab：操作流程/读取规则/回写规则） | supabase/functions/feishu-read/index.ts, src/routes/index.tsx
2026-06-26 | codex | 新增飞书表接入指南：面向新项目说明 Edge Function 读表/回写、secrets、鉴权、单元格解析和常见错误 | docs/FEISHU_SPREADSHEET_INTEGRATION.md, docs/PLAN.md, docs/WORKLOG.md
2026-07-05 | claude | 适配授权码表新列布局：sheet 改名「建联-姓名」（staff_sheets 迁移）；feishu-read 读 A2:W（Q=VID/R=授权码/J=SKU），移除首个非空投放日期行启发式，待办=V 列为空；feishu-writeback 回写 P/Q→V/W；feishu-read-bd-vids 同步改列；前端说明面板与文档更新 | supabase/functions/{feishu-read,feishu-writeback,feishu-read-bd-vids}/index.ts, supabase/migrations/20260705000000_rename_staff_sheets_jianlian.sql, src/routes/index.tsx, docs/{ARCHITECTURE,FEISHU_SPREADSHEET_INTEGRATION,PLAN}.md
2026-09-09 | claude | 发样及素材统计「发样数量 / 回收素材数量（BD）」日图改造：标题去掉「（BD）」；`connection-stats-query` 每日序列新增 `editor_recover`（剪辑素材当日回收，vid 去重，沿用现有 post_date 口径）；第二根柱子改为 BD素材（蓝）+ 剪辑素材（红）堆叠；发样/BD素材/剪辑素材三个图例改为可点击开关，独立控制对应柱子显示与否，坐标轴按当前可见数据重新缩放；回收率折线只反映 BD 口径，当发样和BD素材都被关闭（含只显示剪辑素材）时自动隐藏折线与右轴 | supabase/functions/connection-stats-query/index.ts, src/routes/connection-stats.tsx, src/lib/version.ts
2026-07-08 | claude | 建联表表头再次调整（27 列 A–AA）：feishu-read 改为 P=VID / Q=授权码 / K=SKU / N=视频登记日期，V/W 回写列不变；feishu-read-bd-vids 同步（读 A2:P）；feishu-writeback 无需改动；前端说明面板与文档更新 | supabase/functions/{feishu-read,feishu-read-bd-vids}/index.ts, src/routes/index.tsx, docs/{ARCHITECTURE,FEISHU_SPREADSHEET_INTEGRATION}.md
2026-07-08 | claude | 「获取所有素材」合并原始 code 归档：feishu-read include_done 模式放宽国家/日期校验并合并「授权记录」K3:Q 归档（vid+code 去重，建联表优先）；feishu-writeback 对归档行跳过 V/W 回写只记日志；appendValues 改 INSERT_ROWS→OVERWRITE 防止日志追加顶移同 sheet 的 K:Q 归档区 | supabase/functions/feishu-read/index.ts, supabase/functions/feishu-writeback/index.ts, supabase/functions/_shared/feishu.ts, src/routes/index.tsx
2026-07-06 | codex | 新增 TikTok BC VID 实时状态接口，直接调用 gmv_max/report/get 并返回原始 dimensions/metrics；抽取共享 ttGet 限速重试客户端 | supabase/functions/{_shared/tiktok.ts,gmv-max-sync/index.ts,gmv-max-live-status/index.ts}, docs/{ARCHITECTURE,PLAN,WORKLOG}.md
2026-07-06 | codex | 修复 TikTok BC VID 实时状态接口 Invalid metrics：移除 item 维度不支持的 3 个静态素材指标，保留投放状态及成效指标 | supabase/functions/gmv-max-live-status/index.ts, docs/{PLAN,WORKLOG}.md
2026-07-09 | claude | 修复自动授权 0/0/0 静默失败：authorize-batch 缺少 x-cron-key 放行导致 cron 调用被 401 拒绝（授权未执行、未回写）；补齐 cron bypass；修正 authorize-log RPC 参数名 provided_key→_key；共享 corsHeaders 增加 x-cron-key；authorize-cron 飞书通知附带 errors 摘要（有错误时标题改为「自动授权异常」） | supabase/functions/authorize-batch/index.ts, supabase/functions/authorize-log/index.ts, supabase/functions/_shared/feishu.ts, src/routes/api/public/hooks/authorize-cron.ts
2026-07-10 | claude | 新增 GMV 归因体系：归因引擎（商品卡单列→VID强匹配BD/剪辑→BD昵称瀑布[人工别名>建联保护期归属>VID推断别名]+站点交接按发布时间分段→无建联）；7 张新表+月度聚合 RPC；4 个 Edge Function（sync-creators/run/upload/feishu）；gmv-max-sync META_METRICS 增发布时间候选 metric+posted_at；前端两个 tab（用户视图/管理视图：进度板、Excel 多文件上传「站点 MAX yyyymm.xlsx」、审查面板）；飞书 5 sheet 往返（归因进度/归因审查/GMV目标/站点交接/达人归因表）。待人工：建 sheet、跑迁移、部署 4+1 个函数、分配 tab 权限 | supabase/migrations/20260710000000_gmv_attribution.sql, supabase/functions/{_shared/{cells,attribution,attribution-report,feishu}.ts,attribution-*/index.ts,gmv-max-sync/index.ts}, src/{lib/{adExcel,attributionApi,tabs}.ts,routes/gmv-attribution*.tsx,components/attribution/*}, package.json, docs/{ARCHITECTURE,PLAN,WORKLOG}.md
- 2026-07-11 [codex] Started GMV attribution review implementation: country-scoped identity keys, new Feishu sheet mappings, USD-rate model, goal-group and immutable batch schema; frontend production build passes.
- 2026-07-13 [codex] Fixed GMV attribution runtime exchange-rate loader and exact sidebar active-tab matching; production build passed.
- 2026-07-13 [codex] Removed VID filtering from the GMV Max daily report and relabeled its distinct-VID count as material count.
- 2026-07-13 [codex] 新增只读 GMV Max 原始 CSV 导出：API 测试页支持广告户+日期范围下载，抽取共享分页报表读取器；多国家同月 Excel 全部上传归因后自动展示合并结果。已通过本地生产构建；待 Lovable 部署 Edge Functions 后用真实 Supabase 数据验证。
2026-08-13 | lovable | 修复 TikTok OAuth 回调 URI 在预览环境变成 lovableproject.com 导致白名单校验失败；前端及 OAuth 初始化函数均固定使用 ads-auth 正式回调地址 | src/components/settings/AccountsTable.tsx, supabase/functions/tiktok-oauth-init/index.ts, docs/PLAN.md, docs/WORKLOG.md
2026-08-13 | claude | 广告户加启用/停用开关：advertiser_countries 加 active 字段，国家唯一性只在 active=true 广告户间强制（set_country/新增 set_active 冲突校验）；feishu-read 匹配只取 active 广告户；authorize-batch 加停用广告户防御性拦截；执行授权目标改为「素材列表筛选」AND「待授权账户面板开关」（面板加 Switch，默认拉取后全开，按钮旁加提示文案）；执行授权拉取只读 role=BD 人员；人员表加只读「飞书表格」派生列（BD→主表格，剪辑→剪辑表格）。**待人工**：跑迁移，重新部署 tiktok-connections/feishu-read/authorize-batch 三个 Edge Function | supabase/migrations/20260813120000_advertiser_countries_active.sql, supabase/functions/{tiktok-connections,feishu-read,authorize-batch}/index.ts, src/lib/store.ts, src/components/settings/{AccountsTable,StaffTable}.tsx, src/routes/index.tsx, docs/{PLAN,WORKLOG}.md
2026-08-13 | claude | 修复执行授权大批量素材超时（authorize-batch 单次调用超 150s 被平台 504 掐断）：handleAuthorize 改为前端按 200 条分块、最多 3 块并行提交，按钮显示实时进度「执行授权中 (done/total)」，单块整体失败标记该批为「API错误」可用现有重试机制单独补跑，不改后端 authorize-batch 逻辑 | src/routes/index.tsx
2026-08-13 | claude | 修复 set_country 唯一性校验 bug：之前只看「占用国家的对方」是否 active 就拒绝，没看「本广告户自己」是否 active，导致已停用的广告户改国家也会被误拦；改为仅当本广告户自身启用（或新建默认视为即将启用）时才校验冲突，已停用广告户可随意共享国家，与「同一国家同时只允许 1 个启用广告户」的业务规则对齐 | supabase/functions/tiktok-connections/index.ts
2026-08-13 | claude | 版本号规则固化：新增 AGENTS.md 硬性约定，每次改动都要更新 `src/lib/version.ts` 的 APP_VERSION（格式 MMDD.NNN，全局递增不按日归零），本次改为 0818.003，Claude/Codex/Lovable 三方均需遵守；执行授权状态条改为独立展示在按钮上方，从点击瞬间即显示「授权中/授权完毕 done/total条，结果：授权中X，已授权X（其余状态有结果才显示）」，执行完毕后保留到下次点击，不再依赖分块回调计数；素材列表分页从 50 行/页改为 20 行/页 | AGENTS.md, src/lib/version.ts, src/routes/index.tsx
2026-08-13 | claude | 修复获取素材两按钮误导：之前共用一个 loading 布尔值，点其中一个两个按钮的转圈图标都会转，分不清在跑哪个命令；改为记录具体是哪个（获取所有素材/获取未授权素材）在跑，只有对应按钮转圈，两个按钮在跑的时候仍都保持不可点击；版本号改为 0818.004 | src/routes/index.tsx, src/lib/version.ts
2026-08-13 | claude | 执行授权体验完善：待授权账户面板加「全部启用/全部禁用」批量按钮；推送记录加分页 10 行/页；执行授权完毕后自动对本轮 API错误（含整批调用失败）的素材补跑一轮授权才算最终结果；状态条文字改为右对齐+限宽换行，修复分类多时排版跑到最左边的问题；版本号改为 0818.005 | src/routes/index.tsx, src/lib/version.ts
2026-08-13 | claude | 未设置国家的广告户默认停用：set_country 首次插入 advertiser_countries 记录改为显式 active=false（不再用表默认值，也不再把"未来会不会启用"当成 true 处理），配合前端 AccountsTable 默认展示未配置行为停用；同时提醒之前几次 tiktok-connections 的修复（停用广告户改国家不再报冲突）若线上仍复现，大概率是 Edge Function 还没重新部署到最新代码；版本号改为 0818.006 | supabase/functions/tiktok-connections/index.ts, src/components/settings/AccountsTable.tsx, src/lib/version.ts
2026-08-18 | lovable | 重新部署 tiktok-connections、feishu-read、authorize-batch 三个 Edge Function（广告户启用/停用、active 过滤、停用广告户拦截改动上线）；版本号改为 0818.007 | docs/PLAN.md, docs/WORKLOG.md, src/lib/version.ts
2026-08-19 | claude | 新增 GMV Max 广告组新建 API：TikTok GMV Max 无独立 adgroup 层，新增 gmv-max-adgroup-create 透传 POST `/campaign/gmv_max/create/`（真实新建 campaign+广告组配置，产生真实花费）；_shared/tiktok.ts 加 ttPost（同 ttGet 限速退避）；不做字段校验，需在 API 测试页命令框按 TikTok GmvMaxCreateBody 手填完整 JSON（含 advertiser_id、幂等 request_id），页面加对应说明文案。**待人工**：部署新 Edge Function 后再实测；测试会真实创建 TikTok 广告组，务必确认 store_id/预算等参数无误再执行 | supabase/functions/gmv-max-adgroup-create/index.ts, supabase/functions/_shared/tiktok.ts, src/routes/api-test.tsx, src/lib/version.ts, docs/{ARCHITECTURE,PLAN,WORKLOG}.md
2026-08-19 | lovable | 部署 Edge Function gmv-max-adgroup-create（共享 _shared/tiktok.ts 的 ttPost 一并打包），线上验证返回 401 鉴权正常；PLAN 任务备注更新为已部署；版本号改为 0819.009 | supabase/functions/gmv-max-adgroup-create/index.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-08-19 | claude | 授权记录表改为不去重覆盖：feishu-writeback 每次授权（含同一VID+授权码换广告户重授权、重试）都单独追加一行，不再按 VID+授权码 匹配已有行更新覆盖，完整保留历史；新增 R=广告户名称/S=广告户ID 两列（刻意跳开 J~Q，因为该 sheet 的 K:Q 是历史冻结归档区，写那里会覆盖旧数据）；「读取现有记录」这步只读 A 列算行数/最大序号（不再需要 D/E 做去重），顺带把这步的读取量从 9 列砍到 1 列，降低触发飞书批量接口偶发内部错误的概率；前端 handleWriteback 与 authorize-cron 的回写调用都补上 advertiser_name/advertiser_id 字段；使用说明面板同步更新；版本号改为 0819.010 | supabase/functions/feishu-writeback/index.ts, src/routes/index.tsx, src/routes/api/public/hooks/authorize-cron.ts, src/lib/version.ts
2026-08-19 | claude | 按用户要求把「授权记录」历史归档区从 K:Q 挪到 M:S，腾出 J:K 给新增的广告户名称/ID（A:K 现在是一整块实时记录，J/K 紧接 I 不留缝）：同步改了 feishu-read（获取所有素材合并归档）、attribution-sync-creators（归因数据源）、feishu-writeback（回写目标 J:K + 相关注释）三处读写位置和 index.tsx 使用说明面板文案。**待人工**：部署前必须先把飞书表里 K3:Q 的实际归档数据手动剪切迁移到 M3 起（不迁移的话代码读 M:S 会读空，且新执行记录写 J:K 时不会碰到还留在原位的旧 K:Q 数据但归档功能会失效），迁移完成后再重新部署 feishu-read/feishu-writeback/attribution-sync-creators 三个 Edge Function；版本号改为 0819.011 | supabase/functions/{feishu-read,feishu-writeback,attribution-sync-creators}/index.ts, src/routes/index.tsx, src/lib/version.ts
2026-08-19 | lovable | 按 0819.011 的归档迁移要求，重新部署 feishu-read / feishu-writeback / attribution-sync-creators 三个 Edge Function（授权记录历史归档区 K:Q→M:S、实时记录 J:K 写入逻辑生效）；版本号改为 0819.012 | supabase/functions/{feishu-read,feishu-writeback,attribution-sync-creators}/index.ts, src/lib/version.ts
2026-08-19 | claude | AGENTS.md 加协作规则：每次改动推送后必须按「用户能自己去 Supabase 后台跑的（省 Lovable 积分，给具体 SQL/路径）」vs「必须让 Lovable 执行的（给可直接复制粘贴的完整指令，尤其是 Edge Function 重新部署）」两类列出后续手动步骤，不能让用户自己猜；纯前端/文档改动要明确说不需要做任何事；版本号改为 0819.013 | AGENTS.md, src/lib/version.ts
2026-08-19 | claude | api-test.tsx「通用查询指令」卡片补充 gmv-max-adgroup-create 实测踩坑：request_id 须为可解析 int64 的纯数字字符串（不能用 UUID）、roas_bid/budget 须为数字类型（不能加引号）——均为线上真实报错换来的经验，非文档记载。纯前端文案改动，不需要重新部署 Edge Function，随 Lovable 同步 main 自动生效；版本号改为 0819.014 | src/routes/api-test.tsx, src/lib/version.ts
2026-08-25 | claude | 新增「GMV MAX新建」页面（表单 + Excel 批量）：新 tab 放在素材成效下/GMV归因上；表单含广告户下拉（自动带出 store_id/BC ID）、广告组名称、商品ID多行、ROI、预算、开始/结束时间；Excel 批量走同一套字段+同一个后端接口，附「下载模板」。新 Edge Function `gmv-max-adgroup-batch-create`：结构化输入，自动查 `advertiser_countries.shop_id`/`tiktok_connections.bc_id` 拼 store_id/store_authorized_bc_id，固定 shopping_ads_type=PRODUCT/product_specific_type=CUSTOMIZED_PRODUCTS/optimization_goal=VALUE/deep_bid_type=VO_MIN_ROAS/product_video_specific_type=AUTO_SELECTION，逐行创建、单行失败不影响其余行；抽出 `_shared/gmv-max-adgroup.ts` 给它和原 `gmv-max-adgroup-create`（重构为调用共享函数，行为不变）共用。顺带给 `tiktok_connections` 加 `bc_name` 列，`tiktok-oauth-exchange` 授权时查 `/bc/get/` 拿名称，设置页「TikTok 授权连接」表加 BC 名称/BC ID 两列（只读展示）。**待人工**：① 跑迁移 SQL（见下方 A 类清单）② 部署 5 个 Edge Function（B 类，见清单）③ 给需要用这个功能的非管理员账号在账号管理里勾选 `gmv-max-create` 权限 ④ 真实写调用，首次先用小额预算验证；版本号改为 0819.015 | src/routes/{gmv-max-create.tsx,oauth.tiktok.callback.tsx}, src/lib/{tabs.ts,store.ts,version.ts}, src/components/settings/AccountsTable.tsx, supabase/functions/{gmv-max-adgroup-batch-create,gmv-max-adgroup-create,tiktok-oauth-exchange,tiktok-connection-save,tiktok-connections}/index.ts, supabase/functions/_shared/{gmv-max-adgroup.ts,auth.ts}, supabase/migrations/20260825120000_tiktok_connections_bc_name.sql, docs/{ARCHITECTURE,PLAN,WORKLOG}.md
2026-08-25 | lovable | 部署 5 个 Edge Function：gmv-max-adgroup-batch-create（新增，结构化批量建单）、gmv-max-adgroup-create（重构为复用共享逻辑，行为不变）、tiktok-oauth-exchange（新增 /bc/get/ 查 BC 名称）、tiktok-connection-save（持久化 bc_name）、tiktok-connections（list 返回 bc_name）；共享文件 _shared/tiktok.ts（ttPost）与 _shared/gmv-max-adgroup.ts 随函数一并打包；执行迁移 20260825120000_tiktok_connections_bc_name.sql（tiktok_connections 加 bc_name 列）；线上验证 5 个函数均返回 401 鉴权拦截正常；版本号改为 0825.016 | supabase/functions/{gmv-max-adgroup-batch-create,gmv-max-adgroup-create,tiktok-oauth-exchange,tiktok-connection-save,tiktok-connections}/index.ts, supabase/functions/_shared/{tiktok,gmv-max-adgroup}.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-08-25 | lovable | tiktok-connections 新增 `set_bc` op：支持在设置页「TikTok 授权连接」表直接编辑 BC 名称/BC ID；前端 AccountsTable 增加 BC 名称/BC ID 可编辑输入框与保存/取消按钮；重新部署 tiktok-connections Edge Function 并验证 401 鉴权拦截正常；版本号改为 0825.017 | supabase/functions/tiktok-connections/index.ts, src/components/settings/AccountsTable.tsx, src/lib/version.ts, docs/WORKLOG.md
2026-08-26 | claude | GMV MAX新建页面「Excel 批量新建」交互优化：下载模板右侧改为「上传文件」按钮（隐藏 input + 触发点击）替代原生文件选择框；上传后立即在模板下方渲染预览表格（全部字段逐行可见）；点「执行创建」改为逐行串行调用 gmv-max-adgroup-batch-create（每次 1 行），表格里每行状态实时刷新为创建中/成功/失败（含报错原因），校验不通过的行直接标记跳过而不阻塞其余行提交；新增「下载结果」按钮导出 xlsx（含状态与 CampaignID/错误原因列）；下载模板生成逻辑改为给 A:C 列（广告户ID/广告组名称/商品ID）预设文本格式 z:"@"（避免大数字 ID 被转科学计数法），F:G 列（开始/结束时间）预设 yyyy-mm-dd hh:mm:ss 日期时间格式，预格式化到 200 行对齐批量接口单次上限。纯前端改动，未碰 Edge Function/DB；版本号改为 0826.018 | src/routes/gmv-max-create.tsx, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-08-26 | claude | GMV MAX新建页面「单个新建」表单布局调整：广告户单独一行；下方分左右两栏，左栏是商品ID多行文本框（一行一个，同时兼容中英文逗号分隔，框内自动换行，复用已有 splitIds 未改解析逻辑，加高对齐右栏），右栏竖排广告组名称/ROI/预算/开始时间/结束时间 5 项，与左侧商品ID框左右并排；仅布局与提示文案调整，校验/提交逻辑不变；版本号改为 0826.019 | src/routes/gmv-max-create.tsx, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-08-26 | claude | GMV MAX新建页面二次布局调整（按截图反馈）：单个新建/Excel批量新建两卡改左右并排且整体高度保持一致（grid lg:grid-cols-2 items-stretch）；单个新建内部改回广告户+广告组名称同一行，下方商品ID文本框与ROI/预算/开始时间/结束时间4项左右并排，商品ID用flex-1自适应高度使下边缘自动对齐结束时间框（非硬编码像素）；商品ID提示语拆到独立第二行不再和标题挤在一起换行；版本号改为 0826.020 | src/routes/gmv-max-create.tsx, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-08-26 | claude | GMV MAX新建页面第三次布局调整：撤销两卡左右并排，改回单个新建在上、Excel批量新建在下的上下堆叠；单个新建内部改为第一行6项并排（广告户/广告组名称/ROI/预算/开始时间/结束时间，grid-cols-2 sm:grid-cols-3 lg:grid-cols-6），第二行商品ID多行文本框独占整行（rows=2默认两行高、w-full）；仅布局调整，校验/提交逻辑不变；版本号改为 0826.021 | src/routes/gmv-max-create.tsx, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-08-26 | claude | GMV MAX新建页面底部加两个 TikTok 官方 API 文档快捷链接（新标签页打开）：GMV MAX创建API（create-product-gmv-max-campaigns/v1.3）、GMV MAX修改API（update-a-gmv-max-campaign/v1.3），方便对照官方文档核对字段；纯前端改动；版本号改为 0826.022 | src/routes/gmv-max-create.tsx, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-08-26 | claude | 新增只读诊断 Edge Function gmv-max-identity-get：透传 TikTok GET /gmv_max/identity/get/，供在 API测试页验证某广告户能否查到 BC 已授权的 TikTok 账号身份（为修复「GMV Max 建单缺 identity_list 导致授权所有账号未生效」做前期验证）；store_id 缺省时自动查 advertiser_countries.shop_id；只读，不建单不改数据。**待人工**：部署该 Edge Function 后才能在 API测试页调用；版本号改为 0826.023 | supabase/functions/gmv-max-identity-get/index.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-08-26 | claude | 新增「发样及素材统计」tab（放在素材成效上方，参照 Claude Design「BD素材回收看板」布局）：新缓存表 connection_material_registry（BD 5 张建联表 A2:Q + 剪辑 2 张登记表 B2:G，delete-by-sheet 再插入，只读飞书不回写）；新 Edge Function feishu-read-connection-stats（同步，挂到设置页「同步飞书表」按钮）+ connection-stats-query（聚合查询：发样记录按 handle+country+sku 去重、BD 回收有效日期用登记日期 N 列/剪辑回收有效日期用发布日期、只看 VID 不校验授权码、两个饼图仅统计 BD、粉丝分层按各达人自己国家门槛定档后合并、BD/剪辑日均都按周一到五各1天+周六0.5天等效工作日折算）；新页面 src/routes/connection-stats.tsx（KPI 卡×6、回收明细表可展开国家/SKU、国家占比+粉丝分层双环图联动下钻、每日发样/回收柱线图支持点同事高亮，同事/国家筛选均多选且同事分 BD/剪辑两行+一键全选）；版本号改为 0826.024 | supabase/migrations/20260826130000_connection_material_registry.sql, supabase/functions/{feishu-read-connection-stats,connection-stats-query}/index.ts, src/routes/connection-stats.tsx, src/lib/tabs.ts, src/components/settings/DataSyncCard.tsx, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-08-26 | claude | 发样及素材统计页面布局复核修正（按截图反馈）：①快捷日期从下拉框改为分段按钮（近7/15/30/60天，同参照设计一致的 pill 样式）②「展示明细（国家/SKU）」勾选框从筛选卡移进回收明细卡片自己的标题行③整页改用 `h-full flex flex-col` + 主体网格 `flex-1 min-h-0` 配合 `grid-rows-[minmax(0,0.78fr)_minmax(0,1fr)]` 固定行高，不再随内容撑高导致整页滚动④回收明细表格从原生 `<table>` 改成 flex 行结构（表头/可滚动表体/合计行三段分离，只有表体内部滚动），配合固定网格行高使其底部始终与右侧「发样数量/回收素材数量」卡片底部对齐，不再随数据量动态变化大小⑤两个环形图卡片、每日趋势图卡片内部区域也都改为 `flex-1 min-h-0`，无数据时「暂无数据」占位与有数据时同样大小，不会收缩。纯前端布局改动，未碰后端口径/Edge Function；版本号改为 0826.025 | src/routes/connection-stats.tsx, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-08-27 | lovable | 重新部署三个 Edge Function：gmv-max-identity-get（只读诊断接口）、feishu-read-connection-stats（发样及素材统计同步）、connection-stats-query（发样及素材统计聚合查询）；版本号改为 0826.026 | supabase/functions/{gmv-max-identity-get,feishu-read-connection-stats,connection-stats-query}/index.ts, src/lib/version.ts, docs/WORKLOG.md
2026-08-27 | claude | 发样及素材统计第二轮 UI/性能复核（按截图反馈）：①全局 AppShell 顶部「只显示版本号」的 header 条删除，版本号挪到左侧栏「广告户授权管理」那一块右对齐显示，给所有 tab 都省出约 48px 高度（不止本页受益）②KPI 6 卡改单行（标题左/数值右）③筛选条改两列布局：左列同事（BD/剪辑两行+各自一键全选/取消），右列国家（第一行）+日期（第二行），两列左对齐并排④回收明细表「素材回收率」列的进度条删掉，只留百分比文字（进度条颜色算法在真实数据下观感不对，直接去掉更清楚）⑤两个环形图统一放大到 138px，图例改用 CSS grid 布局（国家占比按图例条数自动切 1/2 列，粉丝分层用 5 列网格对齐各字段），不再出现内部滚动条或文字重叠⑥性能：country/staff 下拉选项列表（脏活，要整表扫描）只在首次进页面时拉一次（`connection-stats-query` 新增 `include_meta` 参数，前端用 ref 记忆是否已拉过，切换筛选条件时不再重复扫整表），每日发样/回收的按日期×按同事聚合从两层 `.filter()` 嵌套改成先按日期分桶的 Map 索引，减少无谓的全表扫描；前端新增 AbortController，切换筛选时自动取消上一个还没回来的请求，避免慢请求把结果覆盖后到；查询/同步期间网格加半透明+同步按钮转圈提示，点击后能立刻看到在动⑦国家大小写在同步时统一转大写（`feishu-read-connection-stats` 里 country 字段 `.toUpperCase()`），修复 MX-SJ / MX-sj 因大小写不同被拆成两个国家的问题（老数据要重新点一次「同步飞书数据」才会用新大小写覆盖）⑧删掉「刷新」按钮，因为切换任何筛选条件本身就会自动重新查询，这个按钮是冗余的。版本号改为 0827.028 | src/components/AppShell.tsx, src/routes/connection-stats.tsx, supabase/functions/{feishu-read-connection-stats,connection-stats-query}/index.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-08-27 | claude | 发样及素材统计第三轮 UI 复核（按截图反馈）：①两个环形图改成响应式撑满卡片可用高度（`h-full aspect-square`，不再是固定 138px），实际观感放大了接近一倍，且用 `xl:grid-cols-[1.2fr_1fr_1fr]` 让国家占比/粉丝分层两张卡片列宽相等，矩形框范围一样大②两个图例都改成 `w-fit` 让每行只占内容实际宽度、列间距用 `auto` 而不是 `1fr` 撑开，不再出现文字和百分比/人数隔很远的问题③颜色算法从固定 12 色数组循环改成黄金角（137.508°）连续取色，避免国家/同事数量超过数组长度时撞色到同一色相，同时给"名字不在列表里"的兜底情况加了字符串哈希取色（原来会因为 `indexOf` 返回 -1 导致颜色算式非法，一批切片同色）④「BD」「剪辑」「全部」（国家）这三个分组勾选按钮改成加粗黑色描边（新增 `Pill` 组件 `strong` 属性），跟下面的具体同事/国家彩色小按钮区分开⑤筛选卡改成 `grid grid-cols-2`，国家+日期这一列固定从卡片正中间开始，不再跟着同事列表长度左右漂移⑥「同事」「国家」「日期」三个标题从灰色改成黑色加粗。纯前端改动，不涉及后端口径/接口；版本号改为 0827.029 | src/routes/connection-stats.tsx, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-08-27 | claude | 发样及素材统计第四轮调整：①筛选卡左右两列对调，改为左列国家+日期、右列同事②回收明细「展示明细」单一勾选框拆成「按国家分组」「按SKU分组」两个独立勾选框，可以任意组合（都不勾=只按人员汇总，只勾国家=人员/国家，只勾SKU=人员/SKU，都勾=人员/国家/SKU）。后端 `connection-stats-query` 把原来的单个 `detail` 布尔参数换成 `group_country`/`group_sku` 两个独立布尔参数，分组 key 和返回行的 country/sku 字段分别按各自开关决定是否带出；前端表格列（表头/数据行/合计行）和分组文案都改成按这两个开关动态拼列表，不再是写死的两套列定义。版本号改为 0827.030 | src/routes/connection-stats.tsx, supabase/functions/connection-stats-query/index.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-08-27 | claude | ①设置页拆分：原「人员表及账号」一个 tab 改成「人员表」「账号管理」两个独立 tab（账号管理仍只对管理员显示）②修复账号管理列表「启用」开关点了没反应的 bug：这个开关之前硬编码 `disabled`，纯展示不可点，唯一生效途径是点编辑铅笔图标→在展开的编辑面板里改「启用」开关→点保存；现在列表里的开关直接可点，点了立即调 `app-accounts` update（乐观更新+失败回滚）不用再进编辑面板。纯前端 + 复用已有 update 接口，未改 Edge Function/DB。版本号改为 0827.031 | src/routes/settings.tsx, src/components/settings/AccountsManager.tsx, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-08-28 | lovable | 重新部署三个 Edge Function：gmv-max-identity-get（只读诊断接口）、feishu-read-connection-stats（发样及素材统计同步）、connection-stats-query（发样及素材统计聚合查询）；线上验证均返回 401 鉴权拦截正常；版本号改为 0828.033 | supabase/functions/{gmv-max-identity-get,feishu-read-connection-stats,connection-stats-query}/index.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-08-27 | claude | 发样及素材统计第五轮调整：①新增 SKU 搜索框（放在筛选卡「同事」下方），按数字整段匹配——输入 333 命中 333-A/AR333/333+311，不命中 3331/3331-A/AR3331（提取 SKU 里所有连续数字段，要求跟查询数字段完全相等，不是子串包含），支持逗号/空格分隔多个数字，400ms 输入防抖；这是全局筛选，在 `connection-stats-query` 新增 `skus` 参数，在国家/同事筛选之后、KPI/回收明细/两个饼图/每日趋势图计算之前先按 SKU 过滤，跟国家、同事筛选口径一致②国家筛选行换行时"第二行贴着卡片左边缘、跟同事看齐"改成"贴着「全部」按钮右侧对齐"：把「全部」按钮从会换行的 flex-wrap 容器里拎出来单独放，国家 chip 列表放进一个独立的内层 flex-wrap 容器，换行只发生在这个内层容器里，各行左边缘都对齐在"全部"按钮右侧、不再跟"国家"两个字对齐。这次改了 Edge Function，需要重新部署；版本号改为 0827.032 | src/routes/connection-stats.tsx, supabase/functions/connection-stats-query/index.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
- 2026-09-05 [lovable] 重新部署 feishu-read-connection-stats、connection-stats-query；核查遗留事项：迁移已跑（connection_material_registry 36678 行）、country 已全部大写（non_upper=0），无需再手动同步覆盖。
2026-09-08 | claude | 修复发样及素材统计 SKU 搜索框：之前的匹配函数只按「查询串本身是纯数字」去比对 sku 的数字段，导致输入 AR333 这种带字母前缀的查询完全不命中（必须手动去掉字母只输 333 才行）。改成按 token 分类匹配——① token 里含数字（"333"或"AR333"，忽略字母部分）按数字整段精确匹配（命中 333-A/AR333/333+311，不命中 3331/AR3331）；② token 纯字母（"B"/"K"）按大小写不敏感子串匹配整个 sku（命中所有含该字母的记录，如 333-B）。前端多 token 分隔符从「逗号/空格」加上「&」（如 333&B = 命中 333 数字整段的记录 OR 含 B 的记录），并在 SKU 输入框右侧加文本提示「用&可合并搜索，如 333&B」。版本号改为 0908.001 | src/routes/connection-stats.tsx, supabase/functions/connection-stats-query/index.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-09-08 | lovable | 重新部署 Edge Function connection-stats-query（SKU 搜索 token 分类匹配逻辑上线）；版本号改为 0908.002 | supabase/functions/connection-stats-query/index.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-09-09 | lovable | 按用户要求重新部署 Edge Function connection-stats-query；版本号改为 0909.004 | supabase/functions/connection-stats-query/index.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-09-09 | claude | GMV 归因 V1 落地（按 GMV_ATTRIBUTION_V1_PLAN.md）：①「GMV 归因」「GMV 归因·管理」月度进度改读 Excel 上传按月合并聚合（不再读官方 API 链路，旧链路代码保留）；②修复同站点同月重复上传导致月度汇总翻倍（create 增加重复检测 + replace_existing 替换）；③修正汇率换算方向（本币/汇率而非×汇率，usd_rate 语义=1美元兑多少本币），finalize 时缺汇率直接报错不写入，设置页新增「GMV 归因汇率」维护 tab；④新增唯一 VID 汇总导出（14 列，按国家×VID×商品ID 重新聚合，PID→SKU 复用 gmv-max-query 的 skuByPid 模式）。接手 codex 2026-07-11 认领但近两月未推进的同名重构任务（文件范围重叠，已在 PLAN.md 标注取代关系）。版本号改为 0909.005。**待人工**：重新部署 Edge Function attribution-upload（无新迁移） | supabase/functions/attribution-upload/index.ts, supabase/functions/_shared/attribution-report.ts, src/routes/{gmv-attribution,gmv-attribution-admin}.tsx, src/components/attribution/UploadView.tsx, src/components/settings/ExchangeRateCard.tsx, src/routes/settings.tsx, src/lib/{api,attributionApi,version}.ts, docs/{PLAN,WORKLOG}.md
2026-09-09 | lovable | 按用户要求重新部署 Edge Function attribution-upload；版本号改为 0909.006 | supabase/functions/attribution-upload/index.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-09-09 | lovable | 按用户要求重新部署 Edge Function attribution-upload、attribution-feishu；版本号改为 0909.008 | supabase/functions/{attribution-upload,attribution-feishu}/index.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-09-09 | lovable | 上传历史加入站点多选下拉框：月份与站点可组合筛选历史列表；解释「按月合并查看」与眼睛图标含义 | src/components/attribution/UploadView.tsx, src/lib/version.ts
2026-09-09 | lovable | 上传状态矩阵固定站点列顺序：PH / TH / VN / MY / SG / PHL / PH2 / MX-AR / MX-NE / MX-SJ / US / JP，未知站点排在末尾 | src/components/attribution/UploadStatusMatrix.tsx, src/lib/version.ts
2026-09-09 | claude | GMV 归因第三轮：①上传页列名修正（GMV→GMV（原币种）/商品卡行→商品卡行数/上传历史 GMV→GMV（USD），并说明「GMV 是金额合计不是行数」）②删掉 Excel 上传页底部冗余的「已生成归因结果/加载中…」提示（归因结果已单列 tab，加载失败时该提示会一直挂着）③暂时关闭站点匹配：昵称类查表新增 lookupIdentity（先「站点+名字」精确、落空按全局键兜底）+ loadAttrContext 写 country='' 全局兜底键 ④暂时关闭 2000 美元 KPI 阈值（KPI_MIN_SITE_USD 2000→0），前端阈值 ≤0 时不显示阈值提示 ⑤定位 4 月归因大面积缺失根因＝站点键对不上（creator_ownership.country 是飞书手填「美国」，上传站点是「US」），昵称路径 100% 落空；顺带修「无建联达人 12 个月趋势表」返回空——unmatched_trend 逐批次拉全列全桶会超时，改成把 attr_bucket 过滤下推数据库 + 只取 3 列 ⑥上传状态矩阵固定站点列（PH/TH 等没数据也保留列，格子留空）。版本号改为 0909.014。**待人工**：重新部署 attribution-upload（必须），attribution-run / attribution-feishu / attribution-sync-creators 建议一并重新部署以同步 _shared 新口径 | supabase/functions/attribution-upload/index.ts, supabase/functions/_shared/{attribution,attribution-report}.ts, src/components/attribution/{UploadView,UploadStatusMatrix,ProgressBoard,StaffCountryTable}.tsx, src/lib/version.ts, docs/{ARCHITECTURE,PLAN,WORKLOG}.md
2026-09-09 | claude | 重新部署 4 个 GMV 归因 Edge Function：attribution-upload、attribution-run、attribution-feishu、attribution-sync-creators，同步 _shared/attribution.ts 与 _shared/attribution-report.ts 新口径上线；版本号改为 0909.015 | supabase/functions/{attribution-upload,attribution-run,attribution-feishu,attribution-sync-creators}/index.ts, supabase/functions/_shared/{attribution,attribution-report}.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-09-09 | claude | GMV 归因第四轮：①更正上一轮错误结论——飞书建联表/剪辑表的站点填的就是 PH/TH/VN/US/MX-AR 代码，与上传文件名同源，「站点写法不一致导致大面积 UNMATCHED」是错的；站点放宽按要求保留但改正了注释与 ARCHITECTURE 里的理由 ②复核 Excel 取数：adExcel 的 toNum 遇到币种符号/不换行空格/全角数字/括号负数会静默记 0，改成统一走新的 parseNumeric 并统计解析失败单元格数 ③上传预览加诊断：GMV 列改为分币种展示、新增「有GMV行数」列，缺货币列/解析失败/多币种/全行 GMV 为 0 四种情况直接告警（缺「货币」列会让所有行默认 USD，非美元站点 GMV 放大几十倍，之前无任何提示）④修 non_usd 误报：把「缺汇率未计入」与「已折算计入」拆开，每项加 usd_rate/gmv_usd，前端分别渲染红色告警与折算明细（PHP xxx ÷ 57 = $xxx）。版本号改为 0909.016。**待人工**：重新部署 attribution-upload、attribution-run、attribution-feishu | src/lib/adExcel.ts, src/components/attribution/{UploadView,ProgressBoard}.tsx, src/lib/{attributionApi,version}.ts, supabase/functions/_shared/{attribution,attribution-report}.ts, docs/{ARCHITECTURE,PLAN,WORKLOG}.md
2026-09-09 | lovable | 按用户要求重新部署 3 个 GMV 归因 Edge Function：attribution-upload、attribution-run、attribution-feishu；共享 _shared/attribution.ts 与 _shared/attribution-report.ts 新口径同步生效；无新 migration | supabase/functions/{attribution-upload,attribution-run,attribution-feishu}/index.ts, supabase/functions/_shared/{attribution,attribution-report}.ts, src/lib/version.ts, docs/WORKLOG.md
2026-09-09 | claude | GMV 归因第五轮：①站点改回按字母精确匹配，删掉上一轮加的跨站点全局兜底（addGlobalFallback），名字对得上但站点对不上的行一律归 UNMATCHED ②新增 site_mismatch action + SiteMismatchTable「站点未匹配确认」表：列出该月 UNMATCHED 里名字在建联归属/别名表登记过（只是登记在别的站点）的达人，含行数/GMV(USD)/登记站点/登记BD/来源，可导出 xlsx，对已归因批次直接生效不用重传 ③「货币」列改必填，缺失时解析直接 throw 阻断上传并给中文提示；空货币单元格与 USD/THB 之外的币种单独告警 ④新增迁移预置汇率 USD=1、THB=32.5（ON CONFLICT DO NOTHING）⑤上传历史加多选框、「全选当前筛选」（跨页）、「删除选中」、「全部删除」（跨页跨筛选清空，二次确认+输入校验），delete action 支持 upload_ids 批量与 all:true。版本号改为 0909.018。**待人工**：跑迁移 20260909160000_seed_thb_exchange_rate.sql；重新部署 attribution-upload、attribution-run、attribution-feishu | supabase/migrations/20260909160000_seed_thb_exchange_rate.sql, supabase/functions/attribution-upload/index.ts, supabase/functions/_shared/{attribution,attribution-report}.ts, src/components/attribution/{SiteMismatchTable,UploadView}.tsx, src/routes/gmv-attribution-admin.tsx, src/lib/{adExcel,attributionApi,version}.ts, docs/{ARCHITECTURE,PLAN,WORKLOG}.md
2026-09-10 | lovable | 修「上传历史-全部删除」报错：原实现是对 ad_uploads 整表级联 DELETE，60 多万行明细连带删除触发 statement timeout；新增 service-role-only 的 public.attribution_uploads_delete_all()（先统计批次数再 TRUNCATE ad_upload_rows + ad_uploads），attribution-upload 的 delete all:true 改走该 RPC 并重新部署。版本号改为 0910.023 | supabase/functions/attribution-upload/index.ts, src/lib/version.ts, docs/WORKLOG.md（migration: attribution_uploads_delete_all）
2026-09-10 | claude | GMV 归因 V2：①大文件上传进度与稳定性——上传队列+上传循环挪到模块级 store（src/lib/uploadQueue.ts），切标签页/切路由回来进度不丢，sessionStorage 镜像让整页刷新后仍能看到上一轮进度表；上传广告表状态列右侧新增「进度」列（百分比 + 已上传行数/总行数 + 当前阶段文案）；分批 1000→2000（服务端上限）、3 并发、单批失败按 1/2/4s 退避重试 3 次（append 以 (upload_id,row_no) 幂等 upsert，重发安全）②finalize 瘦身——只读归因需要的 9 列、attr_* 回填只发「主键 + 4 个归因列」（原来把 22 列原样回传，10 万行必超时，表现就是「卡在上传中」），并加读取/回填耗时日志 ③上传历史对非 READY 批次新增「重新归因」按钮，行数已传完时重跑 finalize 即可，不必重传文件 ④新增 attribution-upload `diagnose` action + 前端「归因口径自查」面板：逐批次统计商品卡/带VID行/VID命中/昵称同站点命中/昵称登记在别站点/从未登记，输出上传站点与建联表站点写法对照和结论提示，用于定位「一个人都归不上」断在瀑布哪一层（WORKLOG 第三轮说站点是「美国」、第四轮说是「PH/TH」，两次结论互相矛盾，此面板以实际数据为准）⑤「数据准备」三个同步按钮补上执行步骤说明。版本号改为 0910.026（0910.025 被 Lovable 同时占用）。**待人工**：重新部署 attribution-upload | src/lib/uploadQueue.ts, src/components/attribution/{UploadView,DiagnosePanel}.tsx, src/routes/gmv-attribution-admin.tsx, src/lib/{attributionApi,version}.ts, supabase/functions/attribution-upload/index.ts, docs/{ARCHITECTURE,PLAN,WORKLOG}.md
2026-09-10 | claude | GMV 归因 V2 补丁：飞书 VID 丢精度防护。飞书把「看起来是数字」的单元格按 JSON number 返回，19 位 TikTok VID（≈7.1e18）超过 JS 2^53 精度，末尾几位被静默改写成 0——字符串仍是「7 开头 19 位」，`^7\d{18}$` 照样通过，于是登记表里存的是错值、VID 强匹配 100% 落空且全程无提示。新增 `_shared/cells.ts` 的 `isPrecisionLostNumber`，`attribution-sync-creators` 三处 VID 读取（建联表 P / 授权记录 Q / 剪辑表 G）统一走 readVid：丢精度的单元格直接丢弃并计数，返回体新增 `registry_vid_rows` 与 `vid_precision_lost`，前端同步完成后给出「请把这三列改成文本格式」的红色提示，并在有效 VID 为 0 时单独告警。版本号改为 0910.027。**待人工**：重新部署 attribution-sync-creators（`_shared/cells.ts` 变更同时影响 feishu-read* 系列，建议一并重部署） | supabase/functions/_shared/cells.ts, supabase/functions/attribution-sync-creators/index.ts, src/routes/gmv-attribution-admin.tsx, src/lib/{attributionApi,version}.ts, docs/WORKLOG.md
- 0910.028 重新部署 attribution-upload（diagnose/finalize 修复）、attribution-sync-creators（VID 丢精度防护）及依赖 _shared/cells.ts 的 feishu-read、feishu-read-sku、feishu-read-editors、feishu-read-bd-vids、feishu-read-connection-stats、attribution-feishu
2026-09-10 | lovable | 修复 attribution-upload finalize 在 10 万行数据下的 statement timeout：确认旧版仅读取 100000 行就耗时 53.8 秒；把 OFFSET 分页改为基于 row_no 的游标分页，并新增 service-role-only 的 attribution_upload_rows_update(uuid,jsonb) RPC，以 UPDATE FROM jsonb_to_recordset 分批回填 4 个归因字段、独立 statement_timeout=120s，替代通用 UPSERT；增加归因各阶段耗时日志。迁移已执行，版本号改为 0910.029 | supabase/functions/attribution-upload/index.ts, supabase/migrations/20260910115818_cb15da33-d4b5-4b39-a890-a8acfa9d5607.sql, src/lib/version.ts, docs/{ARCHITECTURE,PLAN,WORKLOG}.md
2026-09-10 | claude | GMV 归因 V3（按用户第二轮意见）：①**上传与归因解耦**——新增 `ad_upload_agg` 表 + RPC `attribution_build_upload_agg`，finalize 改为「在数据库里按 (VID, 达人昵称, 商品ID, 内容类型, 币种) 归并」，10 万行级压到几千组，**不再计算也不再固化任何归因结果**；`get`/`merged`/明细下钻/`export_vid_summary`/`unmatched_trend`/`site_mismatch`/`diagnose` 全部改成读归并表 + 用当下的 `creator_registry`/`creator_ownership`/`creator_alias`/`site_handovers` 现算，`ad_upload_rows.attr_*` 四列废弃。效果：同步达人登记后刷新报表就是新结果，不用重传广告表、不用重跑批次；顺带解决 finalize 回写 10 万行超时（回写这一步整个没了）②**飞书读取一律按文本**——`readRange` 默认 `valueRenderOption=ToString`，纯数字 ID（19 位 VID、商品ID、授权码）不再被当 JSON number 返回而丢 2^53 以上的精度；`parseDate`（_shared/cells.ts 与 feishu-read 各一份）补「年在前」的精确匹配以适配 ToString 后的文本日期 ③**站点写法统一英文简写**——`create` 拒收含汉字的站点并提示改文件名，`attribution-sync-creators` 统计建联/剪辑表里的汉字站点写法返回 `cjk_sites`，前端红字提示回飞书改正，诊断面板也增加对应结论；不做任何汉字站点的匹配或换算 ④「数据准备」三个按钮的长说明改成按钮右侧「?」弹出（Popover），卡片头部保留一句口径提醒。版本号改为 0910.028。**待人工**：跑 migration `20260910120000_ad_upload_agg.sql`；重新部署 attribution-upload、attribution-sync-creators、attribution-feishu、feishu-read、feishu-read-sku、feishu-read-editors、feishu-read-bd-vids、feishu-read-connection-stats、feishu-writeback、attribution-run | supabase/migrations/20260910120000_ad_upload_agg.sql, supabase/functions/attribution-upload/index.ts, supabase/functions/attribution-sync-creators/index.ts, supabase/functions/_shared/{feishu,cells,attribution}.ts, supabase/functions/feishu-read/index.ts, src/routes/gmv-attribution-admin.tsx, src/components/attribution/{UploadView,DetailTable,DiagnosePanel}.tsx, src/lib/{attributionApi,uploadQueue,version}.ts, docs/{ARCHITECTURE,PLAN,WORKLOG}.md
2026-09-10 | claude | GMV 归因 V4（按用户第三轮意见）：①**两层存储**——第一层仍是 `ad_upload_agg`（达人昵称/VID 维度的原始归并数据），新增第二层 `attribution_runs`+`attribution_run_rows`：一次「全站点全人员」归因的结果快照 + 明细，带历史（RPC `attribution_runs_prune` 每月只留最近 10 条）。新增 `refresh`（生成快照，cron 带 x-cron-key 免口令）/`report`（读该月最新 READY 快照）/`report_detail`（从快照明细表下钻，不重算）/`runs`（快照历史）四个 action ②**每晚自动刷新**——新增服务端路由 `/api/public/hooks/attribution-cron` + migration 把 pg_cron 任务排在北京 23:30（复用已有 authorize-cron 任务里的域名与 apikey，不再把密钥写进迁移），默认刷新最近 3 个有已完成批次的月份；上传完成后也会自动刷新该月快照；页面上保留「重新计算」手动刷 ③前台由「每次即时全量重算」改为「读最新快照」，页面显示快照时间/来源/触发人和批次-原始行-归并组量级，并提供「快照历史」弹窗 ④**页面状态不再丢**——两个归因页面的月份、报表、明细统一放进模块级 store `src/lib/attributionView.ts` + sessionStorage 镜像，切标签页、切路由、整页刷新都能还原。版本号改为 0910.031。**待人工**：跑 migration `20260910140000_attribution_runs.sql` 与 `20260910150000_attribution_cron.sql`；重新部署 attribution-upload | supabase/migrations/{20260910140000_attribution_runs,20260910150000_attribution_cron}.sql, supabase/functions/attribution-upload/index.ts, src/routes/api/public/hooks/attribution-cron.ts, src/routes/{gmv-attribution,gmv-attribution-admin}.tsx, src/lib/{attributionView,attributionApi,uploadQueue,version}.ts, docs/{ARCHITECTURE,PLAN,WORKLOG}.md
2026-09-11 | claude | GMV 归因 V5（按用户第四轮意见）：①**修「同步达人登记后重新计算仍然全 0」**——根因是解耦改造之前 finalize 过的历史批次只有 ad_upload_rows、没有 ad_upload_agg 行，现算时读到 0 条归并数据。新增 `ensureAgg`：生成快照和归因口径自查时，自动为「已 READY 但归并表为空」的批次补跑一次 `attribution_build_upload_agg`，不需要逐个点「重新归并」；自查结论里会说明补跑了哪几个批次 ②**cron 不再每晚白算**——`refresh` 新增 `skip_if_unchanged`（cron 默认开、手动重算永远强制）：该月没有新完成的广告表批次、且 creator_ownership 没有更新时直接跳过，只花两条极小的查询；保留北京 23:30 的频率 ③**归并口径收口**——新 migration 把归并 GROUP BY 显式带上站点（站点×VID×达人昵称×商品ID×内容类型×币种）；归并层只存可相加的数值（GMV/成本/订单/曝光/点击），ROI/CTR/CVR 一律在出报表时用汇总后的分子分母重算 ④**站点写法**——新增 `normalizeSiteCode`（等效写法映射，PH本土→PHL）与 `isHeaderLikeSite`（「地区/店铺」「国家」「（空）」等表头/占位不再被当成汉字站点误报），同步达人登记与自查两处的告警都按新规则过滤 ⑤**上传列表可干预**——页面刷新后不再恢复「待上传」这类既传不了也删不掉的残留行，其余条目每行新增移除按钮 ⑥按要求删除已执行的 3 个 migration 文件（20260910120000/20260910140000/20260910150000），表结构与 RPC 定义改记录在 docs/ARCHITECTURE.md「GMV 归因两层存储（表结构备查）」一节。版本号改为 0911.034。**待人工**：在 SQL Editor 执行 `20260911090000_agg_group_by_site.sql` 后删除该文件；重新部署 attribution-upload、attribution-sync-creators | supabase/migrations/20260911090000_agg_group_by_site.sql, supabase/functions/attribution-upload/index.ts, supabase/functions/attribution-sync-creators/index.ts, supabase/functions/_shared/attribution.ts, src/lib/{uploadQueue,attributionApi,version}.ts, src/components/attribution/UploadView.tsx, docs/{ARCHITECTURE,PLAN,WORKLOG}.md
2026-09-11 | claude | GMV 归因 V6（按用户第五轮意见）：①**修「重新计算失败：Edge Function returned a non-2xx」**——根因是归并压缩率远低于预期（实测 10 万原始行 → 9.4 万归并行，因为归并键含商品ID），整月 8~12 个批次几十万行全部拉进 Edge Function 再算再写回，必然超时/爆内存。改成「判定在 Edge Function、聚合在数据库」：新增 RPC `attribution_month_keys`/`attribution_month_key_count` 只吐出去重后的判定键（站点×VID×达人昵称×内容类型，判定与金额无关），引擎跑完把结果写进新表 `attribution_run_keys`，再由 `attribution_apply_run` 在数据库内 JOIN 归并表+汇率，写出 VID 粒度的快照明细并返回「几百行」的紧凑汇总，Edge Function 只做 JSON 整形（新增 `buildReportFromCompact`）②**归因口径新增去重计数**：站点×同事的归因 VID 数与归因达人昵称数由 SQL 的 count(DISTINCT) 算出，与 GMV 并列展示（进度板总计 + 同事表格子）③**新增「数据准备进度」面板**：达人登记一人一行 × 固定站点列（PH TH VN MY SG PH2 PHL MX-AR MX-NE MX-SJ US JP，没数据也占位，格子为「VID数 / 达人数」）、当月 GMV 目标（按 BD/剪辑分列 + 合计）、站点交接列表（站点 / 原BD→新BD / 日期 / 备注），对应新增 `registry_matrix`/`targets`/`handovers` 三个 action 与 RPC `attribution_registry_matrix` ④建联表按表头正则自动识别「粉丝量/粉丝数/followers」列，解析「12.3万/1.2w/850K」等写法写入 `creator_registry.follower_count` 与 `creator_ownership.follower_count`，前台暂不展示 ⑤上传按钮改名「上传解析数据」，上传历史默认显示「上传状态矩阵」⑥`site_mismatch`/`unmatched_trend`/`get(merged)` 全部改为读快照不重算，`diagnose` 改为按判定键统计并按站点分组（原来逐批次拉全部归并行同样会超时），`export_vid_summary` 改流式聚合避免整月驻留内存 ⑦快照清理改为「每月保留最近 N 条 summary，但只有最新一条保留可下钻明细」，避免明细表被几十万行×多条快照撑爆。版本号改为 0911.037。**待人工**：在 SQL Editor 执行 `20260911120000_attribution_snapshot_sql.sql` 后删除该文件；重新部署 attribution-upload、attribution-sync-creators | supabase/migrations/20260911120000_attribution_snapshot_sql.sql, supabase/functions/attribution-upload/index.ts, supabase/functions/attribution-sync-creators/index.ts, supabase/functions/_shared/attribution-report.ts, src/components/attribution/{DataPrepPanel,DiagnosePanel,StaffCountryTable,ProgressBoard,UploadView}.tsx, src/lib/{attributionApi,version}.ts, docs/{PLAN,WORKLOG}.md
2026-09-11 | lovable | 复核项目监控：大文件上传超时告警引用的旧全量读取/回填流程已被数据库归并与快照聚合替换，近期线上无同类错误，判定过时；修复「清空全部批次」RPC 漏掉 ad_upload_agg 导致外键阻断，新增迁移同时 TRUNCATE ad_upload_agg、ad_upload_rows、ad_uploads，权限仍仅 service_role；版本号改为 0911.038 | supabase/migrations/20260911025539_2fed9b74-7034-4696-98a7-6104ca83f45d.sql, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-09-11 | claude | GMV 归因 V7（按用户第六轮意见）：①**金额在归并时就折美元**——`ad_upload_agg` 新增 `usd_rate`/`gmv_usd`/`cost_usd`，`attribution_build_upload_agg` 在归并那一刻用后台维护的汇率折算并把当时用的汇率一起存下来（可追溯、可重算）；`attribution_apply_run` 随之简化为只加 USD、不再 JOIN 汇率表，币种字段退化为展示标记（单一币种显原币种、混合显 MIXED）；新增 RPC `attribution_rebuild_agg_usd(month)` 在汇率改动后就地重算该月美元金额（不用重传文件），生成快照时自动先调一次，所以「改汇率 → 重新计算」直接生效；迁移里同时把存量归并行按当前汇率补算了一次 ②**去重计数口径固定为「只按 (同事, 国家)」**——新增 RPC `attribution_run_distinct`，用 GROUPING SETS 一次算出 CELL(同事×角色×国家) / STAFF(同事×角色) / TOTAL 三个粒度的精确 `count(DISTINCT vid)` 与 `count(DISTINCT 归一化昵称)`，统计范围是 bucket='STAFF' 的行；`attribution_apply_run` 不再返回分格子的 vids/creators（恒为 0），`buildReportFromCompact` 删掉原先在 JS 里「格子取 max、合计相加」的估算——那个做法在拆分行 VID 重叠时高估、不重叠时低估，两头都不准。因为快照明细本身就是整月多文件合并后的 (站点,VID,昵称,内容类型) 粒度，所以同月两个 PH 文件（同币种/不同币种/换过广告户）里的同一个 VID 天然只算一次 ③顶部「归因 VID 数 / 归因达人数」改为只统计真正归到人的部分（原来把商品卡和无建联的键也算进去了，与指标名不符）④`export_vid_summary` 改用归并层已折好的美元金额。版本号改为 0911.039。**待人工**：在 SQL Editor 执行 `20260911170000_agg_usd_and_distinct.sql` 后删除该文件；重新部署 attribution-upload | supabase/migrations/20260911170000_agg_usd_and_distinct.sql, supabase/functions/attribution-upload/index.ts, supabase/functions/_shared/attribution-report.ts, src/components/attribution/{StaffCountryTable,ProgressBoard}.tsx, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-09-11 | claude | GMV 归因 V8（按用户第七轮意见）：①**修 migration 执行报错**——`ERROR 42P10: invalid reference to FROM-clause entry for table "a"`：`UPDATE ... FROM LATERAL (...)` 里不能反向引用被更新的表，`attribution_rebuild_agg_usd`、存量补算 UPDATE、以及 `attribution_build_upload_agg` 三处统一改成「先把『币种 → 汇率』做成一张小表再按币种 JOIN」。因为该迁移尚未执行，直接改原文件而不是再加一个 ②**内容类型四分类，所有行都入库不筛**：视频 = VID 强匹配（BD/剪辑都可）+ 达人昵称（仅 BD）；直播 = **只按达人昵称归 BD，不走 VID 强匹配**；商品卡 = PRODUCT_CARD 桶不归人；其他 = 新增 **OTHER 桶**不归人。`normalizeCreativeType` 原来把认不出的类型默认当成「视频」，会把其他类型的 GMV 混进视频口径，现在一律归 other。商品卡与其他的 GMV/成本照样入库统计，只是不参与归人 ③新增 RPC `attribution_run_by_type`（站点 × 内容类型 × 桶）并把结果挂进快照 summary.by_type，前端新增「内容类型占比」面板：各站点 GMV 按视频/直播/商品卡/其他的金额与站内占比，底部一行全站点合计 ④进度板新增「其他类型」卡片（可下钻），归因口径自查新增「其他类型」列，明细表类型标签补 other。版本号改为 0911.040。**待人工**：按顺序执行 `20260911090000_agg_group_by_site.sql` → `20260911120000_attribution_snapshot_sql.sql` → `20260911170000_agg_usd_and_distinct.sql`，执行完删除；重新部署 attribution-upload | supabase/migrations/20260911170000_agg_usd_and_distinct.sql, supabase/functions/_shared/{attribution,attribution-report}.ts, supabase/functions/attribution-upload/index.ts, src/components/attribution/{TypeMixPanel,ProgressBoard,DiagnosePanel,DetailTable}.tsx, src/lib/{attributionApi,version}.ts, docs/{PLAN,WORKLOG}.md
2026-09-11 | claude | 修 migration 执行「Server Error」：原 `20260911170000_agg_usd_and_distinct.sql` 里有一条**全表** UPDATE（给几十万行 ad_upload_agg 补算美元金额），SQL 编辑器等不到它跑完就断开，整个脚本在一个事务里于是全部回滚，报的是没有 Postgres 错误码的「Server Error」（与上一次的 42P10 是两回事）。拆成三个可分别执行的小文件：`20260911170000_agg_usd_columns.sql`（只加 usd_rate/gmv_usd/cost_usd 三列 + 一个部分索引，秒级）、`20260911170100_agg_usd_functions.sql`（attribution_build_upload_agg 归并即折美元、attribution_rebuild_agg_usd 按月重算，新增 **attribution_backfill_agg_usd(_limit)** 分批补算存量、每次独立事务、重复执行到返回 0 即可）、`20260911170200_attribution_distinct_and_type.sql`（attribution_apply_run 只加 USD、attribution_run_distinct 按 (同事,国家) 精确去重、attribution_run_by_type 内容类型占比）。全表 UPDATE 从迁移里删掉——生成快照时本来就会按月调 attribution_rebuild_agg_usd 自动补。版本号改为 0911.041 | supabase/migrations/{20260911170000_agg_usd_columns,20260911170100_agg_usd_functions,20260911170200_attribution_distinct_and_type}.sql, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
2026-09-11 | lovable | 按顺序执行 3 个迁移（ad_upload_agg 加 usd_rate/gmv_usd/cost_usd + 部分索引；attribution_build_upload_agg 归并即折美元、新增 attribution_backfill_agg_usd 分批补算；attribution_apply_run 只加 USD、新增 attribution_run_distinct 与 attribution_run_by_type），6 个函数确认存在；重新部署 attribution-upload（内容类型四分类、去重计数按同事+国家、by_type 挂进快照）；版本号 0911.042 | supabase/migrations/2026091117*.sql, src/lib/version.ts, docs/WORKLOG.md
2026-09-11 | claude | ①**GMV 目标 / 站点交接改读绩效配置表格**——这两项的「绩效配置表」在另一个飞书表格（WOVdw1QOSiW4iVk1bYoclPEDnRe），不是主表格。`attribution-feishu` 把 sheet 解析抽成 `makeSheetResolver`，主表格与绩效表格各建一个；`sync-targets`(A2:F) 与 `sync-handovers`(H2:L) 改读绩效表格，返回体带上实际用的 spreadsheet token 便于核对；可用 secret `FEISHU_PERF_SPREADSHEET_TOKEN` 覆盖（填 token 或整条 sheets 链接都行），找不到 sheet 时把该表格现有 sheet 名全列出来 ②**新增「金额对账」面板**（排查「数据丢到哪了」）：新 RPC `attribution_month_reconcile(month, run_id)` 把归并层 `ad_upload_agg`（按内容类型）与快照层 `attribution_run_rows`（按内容类型 × 桶）并排返回，前端 `ReconcilePanel` 显示上传原始行/归并行、两层 GMV 合计与差额（差额 > 1 美元标红）、缺汇率组数告警、以及逐批次的原始行数与金额。两层合计不等就是中间丢了，差在哪个内容类型一眼可见。版本号改为 0911.044。**待人工**：执行 `20260911180000_attribution_reconcile.sql`；重新部署 attribution-feishu、attribution-upload | supabase/migrations/20260911180000_attribution_reconcile.sql, supabase/functions/{attribution-feishu,attribution-upload}/index.ts, src/components/attribution/ReconcilePanel.tsx, src/routes/gmv-attribution-admin.tsx, src/lib/{attributionApi,version}.ts, docs/WORKLOG.md

2026-09-13 | claude | **站点交接改为「新 BD 动作触发」**：交接表上的日期不再等于转移生效日，只是「允许转移」的起点。实际转移日 = 新 BD 在交接日（含）之后，对那个具体达人第一次登记/发样的日期——新 BD 从没接手过该达人，这条交接对他永不生效（不再因为交接日到了就把历史达人批量转给新人）；发布时间 < 转移日仍算原 BD，≥ 转移日才切新 BD；发布时间未知按当前归属处理。只作用于昵称/账号路径，VID 强匹配不受影响。`loadAttrContext` 新增 `creatorActions`（站点×达人 → 同事 → 登记动作日期升序，取 register_date，没有则退回 sample_date）；`isHandoverBoundary` 的 ±5 天 VID 推断审查提示改为围绕实际转移日而非交接表日期。本轮是本次计划里唯一会改变归因结果的改动，单独提交。版本号 0913.045。**待人工**：重新部署 attribution-upload（无 migration） | supabase/functions/_shared/{attribution,attribution-report}.ts, docs/{GMV_ATTRIBUTION_REVIEW_PLAN,PLAN,WORKLOG}.md, src/components/attribution/DataPrepPanel.tsx, src/routes/gmv-attribution-admin.tsx, src/lib/version.ts

2026-09-13 | claude | **新增归属五分类枚举函数**：`OwnershipClass` = 商品卡片 / 剪辑 / BD-VID / BD-账号 / 未建联达人 / 其他，`classifyAttribution(bucket, role, matchType)` 把系统内部的 (桶, 角色, 匹配方式) 折成月度 Excel 流程「归属角色」列的那几档，避免两边对数据时互相翻译。「其他」是本系统后加的桶（创意类型认不出来的行），Excel 流程里没有对应项。本次只落函数，不接线、不改现有判定与展示。版本号 0913.046。**不需要人工操作**（无 migration、无 Edge Function 行为变化） | supabase/functions/_shared/attribution.ts, docs/{PLAN,WORKLOG}.md, src/lib/version.ts

2026-09-14 | claude | 新增 `docs/GMV_ATTRIBUTION_DIAGNOSE_SQL.md`：GMV 归因排查 SQL 清单，按 批次表 → 归并层 → 快照层 → 登记/归属表 四层逐段给出可直接在 SQL Editor 执行的查询（数据是否还在、快照把钱判到哪个桶、广告表与登记表的站点写法比对、VID 强匹配与昵称路径各自的命中率、缺汇率币种、内容类型金额分布、原始归并行抽样），每段附「怎么读」结论。不改任何代码逻辑。版本号 0914.048。**不需要人工操作** | docs/{GMV_ATTRIBUTION_DIAGNOSE_SQL,WORKLOG}.md, src/lib/version.ts

2026-09-14 | claude | 排查 SQL 清单新增 G 段「用旧快照回测归因逻辑」：广告批次被删后 `attribution_run_rows` 仍保留每条判定键的 country/vid/account_name/bucket，可拿当前 creator_registry / creator_ownership 回测旧快照里判为 UNMATCHED 的键，区分「判定逻辑接错了」「站点写法对不上」「确实没建联」三种情况，不必先重传 Excel。版本号 0914.049。**不需要人工操作** | docs/{GMV_ATTRIBUTION_DIAGNOSE_SQL,WORKLOG}.md, src/lib/version.ts

2026-09-14 | claude | **修分页读取漏行**：`attribution-report.ts` 的 pageAll 与 `attribution-upload` 的 pageAllRows 此前用 LIMIT/OFFSET 翻页却不带 ORDER BY，Postgres 在这种写法下不保证两次请求的行序一致（并行顺序扫描尤其会变），翻页之间既可能重复也可能整段漏行；creator_registry 六万行要翻 61 页，漏掉登记数据的直接表现就是「大批达人归不上」。11 处调用全部补上稳定排序（各表主键，gmv_exchange_rates 按 currency），pageAllRows 增加 orderBy 参数并在注释里写明调用方必须带排序。另在排查 SQL 文档补 H 段「快照与登记数据谁先谁后」，用于判断回测结论是否成立。版本号 0914.050。**待人工：重新部署 attribution-upload** | supabase/functions/_shared/attribution-report.ts, supabase/functions/attribution-upload/index.ts, docs/{GMV_ATTRIBUTION_DIAGNOSE_SQL,WORKLOG}.md, src/lib/version.ts

2026-09-14 | claude | **定位并修复「除 JP 外全部归不上」的真因**：PostgREST 服务端单次返回行数上限（Supabase 默认 1000）对 RPC 一样生效，`attribution_month_keys` 传 `_limit: 5000` 实际只回 1000 行；而翻页结束条件写的是「本页行数 < 请求页大小」，第一页就被当成读完。整月 167,895 个判定键只判了前 1,000 个，其余在 `attribution_apply_run` 的 `LEFT JOIN … coalesce(k.bucket,'UNMATCHED')` 里静默兜成无建联。判定键按 country 排序、JP 排第一，于是表现成「只有 JP 有归因」。算术完全吻合：JP 共 1,369 键 = 前 1,000 被判定（649 归到人 + 351 无建联）+ 369 未判定，合计无建联 720，与快照一致。修法：抽出 `fetchMonthKeys`（页大小对齐上限、按实际返回行数推进 offset、空页结束、与 `attribution_month_key_count` 对账，数量不符直接报错），诊断面板同一处翻页一并改掉；新增 `assertNotCapped` 守住 apply_run/distinct/by_type 三个汇总 RPC。版本号 0914.051。**待人工：重新部署 attribution-upload** | supabase/functions/attribution-upload/index.ts, docs/{PLAN,WORKLOG}.md, src/lib/version.ts

2026-09-14 | claude | 上传状态矩阵格子左侧显示「已归因（READY）文件数」：数字绝对定位在格子左内侧，状态文字仍按整格居中，数字位数变化不会把文字推偏；tooltip 首行补「已归因 x / 共 y 个文件」。纯前端展示调整。版本号 0914.052。**不需要做任何事，Lovable 同步 main 后自动生效。** | src/components/attribution/UploadStatusMatrix.tsx, docs/WORKLOG.md, src/lib/version.ts

2026-09-14 | claude | **修大文件「归并失败：upstream request timeout」+ 新增失败批量重试**：10 万行的归并在一次 HTTP 请求里做不完，而整个归并是一个事务，网关超时即回滚——批次永远卡在「上传中」，重试多少次都一样。新增 migration `20260914120000_finalize_chunked.sql`，把归并拆成三步：`attribution_clear_upload_agg`（清上一轮）→ `attribution_build_upload_agg_part`（按归并键哈希取模分片，同一归并组永远落在同一片，不会被切开重复计数；`ON CONFLICT DO NOTHING` 让同片重跑幂等）→ `attribution_finalize_mark`（汇率校验、回填合计、置 READY）。每片是独立事务、独立一次请求，超时也只丢那一片。Edge Function 的 finalize 在原始行数 > 2 万时自动转分片并返回 `{staged, parts}`；`uploadApi.finalize` 内部逐片循环调用，对调用方签名不变；上传队列阶段文字与「重新归并」按钮都会显示第几片。小文件仍走原来的一次性 `attribution_finalize_upload`。前端上传历史新增「失败重试（N）」按钮：对全部未完成批次串行重跑归并，行数据还在库里不必重传，单个失败不影响后续，结束后汇总成功/失败数。版本号 0914.053。**待人工：跑 migration 20260914120000_finalize_chunked.sql；重新部署 attribution-upload** | supabase/migrations/20260914120000_finalize_chunked.sql, supabase/functions/attribution-upload/index.ts, src/lib/{attributionApi,uploadQueue,version}.ts, src/components/attribution/UploadView.tsx, docs/{PLAN,WORKLOG}.md

2026-09-14 | claude | 订正 ARCHITECTURE 里一句过时描述：上传重复判定的粒度是**文件**（country+month+file_name）不是月份，`replace_existing` 也只按这三者删旧再插；同站点同月的多个分段文件各自独立成批次，报表按月相加、互不覆盖。原文写成「同 country+month 的记录不可重复」，容易被误读成同月第二个分段文件会顶掉第一个。仅文档订正，无代码改动。版本号 0914.054。**不需要做任何事。** | docs/{ARCHITECTURE,WORKLOG}.md, src/lib/version.ts

2026-09-14 | claude | **上传阶段的状态文案改回它真正在做的事**：归因自 2026-09-10 解耦后已不在上传这一步计算，但状态标签还写着「归因中」「已归因」，容易让人以为在等归因。改为「归并中」「已入库」（状态矩阵同步改为「已入库 / 部分入库」），并在「归并中」标签加 tooltip 说明这一步只是把该文件的行按站点+VID+达人昵称+商品ID+内容类型+币种合并成组、按后台汇率折美元存库，归因是出报表时按当下飞书登记数据现算。另外进度条在归并阶段不再死卡 95%：分片归并时按已完成片数从 90% 走到 99%，阶段文字区分「第 N / M 片」与「收尾：核对汇率、回填合计」。纯前端文案与进度展示调整。版本号 0914.056。**不需要做任何事，Lovable 同步 main 后自动生效。** | src/components/attribution/{UploadView,UploadStatusMatrix}.tsx, src/lib/{uploadQueue,version}.ts, docs/WORKLOG.md

2026-09-14 | claude | 按用户要求去掉上传历史的「失败重试」批量按钮（重试仍然失败，留着只是反复浪费时间），改为强化清除：原「一键清除卡住记录」要求批次创建超过 15 分钟才算卡住，结果刚失败的批次按钮是灰的、反而干预不了；现改名「清除未完成记录（N）」，覆盖全部非 READY（上传中 / 失败）批次，随时可点，删完重新上传即可。单行的「重新归并」按钮保留不变。版本号 0914.057。**不需要做任何事，Lovable 同步 main 后自动生效。** | src/components/attribution/UploadView.tsx, src/lib/version.ts, docs/WORKLOG.md
- 0914.064 重算进度（refreshing/已等待秒数）从组件 state 挪进 attributionView 模块缓存，切 TAB/切路由回来进度不丢；整页刷新时复位避免卡转圈

2026-09-14 | claude | **修「快照重算超时」：瓶颈是请求次数不是 SQL**。PostgREST 单次最多返回 1000 行，16 万+ 判定键读出来要 168 次 HTTP 往返，判定结果按 2000 行一批写回又是 84 次，250 多次串行往返超过 Edge Function 后台任务的墙钟上限；进程被掐断后 `attribution_runs` 那行永远停在 RUNNING，前端轮询拿不到 READY/FAILED，只能等满 20 分钟报超时。新增 migration `20260914160000_snapshot_bulk_io.sql`：①`attribution_month_keys_json` 把整页打成一个 JSON（单行）返回，绕开 1000 行上限，一次拉两万；②`attribution_write_run_keys(_run_id, _rows jsonb)` 一次写两万；两项合计把往返次数从 250+ 压到 20 上下 ③`attribution_runs_fail_stale(_minutes)` 给死掉的 RUNNING 快照收尸，标 FAILED 并写明「后台重算进程已中断」，`refresh` 开头和 `runs` 轮询各调一次，前端几分钟内就能看到真实结论。版本号 0914.058。**待人工：跑 migration 20260914160000_snapshot_bulk_io.sql；重新部署 attribution-upload** | supabase/migrations/20260914160000_snapshot_bulk_io.sql, supabase/functions/attribution-upload/index.ts, docs/{PLAN,WORKLOG}.md, src/lib/version.ts

2026-09-14 | lovable | 执行 migration `20260914160000_snapshot_bulk_io.sql`：新增 `attribution_month_keys_json`/`attribution_write_run_keys` 两个 JSON 批量 IO 函数与 `attribution_runs_fail_stale` 死快照清理函数；重新部署 Edge Function `attribution-upload` 使新函数生效。版本号 0914.065。**无需额外人工步骤** | supabase/migrations/20260914160000_snapshot_bulk_io.sql, supabase/functions/attribution-upload/index.ts, docs/{PLAN,WORKLOG}.md, src/lib/version.ts

2026-09-14 | claude | **快照重算的真因是 Edge Function 的 CPU 配额**（日志 `CPU Time exceeded`），不是网关超时：2026-08 共 250,296 个判定键，读取已压到 47 秒 / 13 次往返，但把 25 万个键解析成 JS 对象、跑引擎、再序列化写回，超出单次调用 CPU 预算，进程被掐断，快照行停在 RUNNING。新增 migration `20260914180000_snapshot_candidate_keys.sql`：把「这条键有没有可能归到人」的存在性判断下沉到数据库——`attribution_seed_run_keys` 一次扫描分两路，非候选键（VID 对不上且昵称查无此人）按内容类型直接在库内定桶写入 `attribution_run_keys`（商品卡→PRODUCT_CARD、认不出→OTHER、其余→UNMATCHED，与引擎对这些键的判定完全一致），候选键写进落地表 `attribution_run_candidates`，Edge Function 只分页取候选键交给引擎。候选判据刻意偏宽、并与引擎逐条对齐（VID 命中 creator_registry/staff_vid_map；昵称命中 creator_ownership/creator_alias；或落在别名投票名单内——引擎会从 VID 命中的视频行给同名达人推别名，这些名字必须算候选）；昵称/站点/内容类型三个归一化 SQL 函数与 TS 的 normalizeName/identityKey/normalizeCreativeType 一一对应，NFKC 用 PG 的 `normalize()` 对齐。判定逻辑仍然只有 TS 引擎一份。版本号 0914.066。**待人工：跑 migration 20260914180000_snapshot_candidate_keys.sql；重新部署 attribution-upload** | supabase/migrations/20260914180000_snapshot_candidate_keys.sql, supabase/functions/attribution-upload/index.ts, docs/{PLAN,WORKLOG}.md, src/lib/version.ts

2026-09-14 | claude | **判定改为按站点分片执行**（用户提出的方式：做完一个国家就存库、记进度、再继续）。前一轮的候选筛选被用户的 Excel 透视对账证伪——2026-08 共 250,275 条素材，真正归到人的占 38.9%（MX-AR 65.3%、MX-NE 78.4%、MX-SJ 76.4%、US 71.3%、JP 70.0%），候选最多压到约 12 万，仍然超 Edge Function 的 CPU 配额；顺带确认「判定键」与用户表里的「素材数」口径一致（250,296 vs 250,275），用户表的「图片」474 条对应系统的「其他」桶。新增 migration `20260914200000_snapshot_chunked_judge.sql`：`attribution_seed_run_keys` 增加分片编号（窗口函数按 (站点, 归一化昵称) 分组累加行数后整除片大小，**同一 (站点,昵称) 必落同一片**——别名投票只在组内产生和消费，因此分片判定与一次性判定结果等价）；`attribution_next_chunk_json` 取下一片、`attribution_drop_chunk` 判完即删（剩余行数就是进度，中途失败重跑只重做没删掉的部分）；`attribution_vid_regs_for_country` 补回「VID 登记在别站点」的登记行，保证上下文收窄后 VID 强匹配的跨站点语义不变。`loadAttrContext` 增加 scope 参数按站点收窄登记/归属/别名/交接/登记动作。`refresh` 拆成 建分片 / judge / finish 三个 stage，前端 `refreshAsync` 与 cron 路由各自驱动循环（不能在 Edge 里循环，CPU 会累加）。版本号 0914.067。**待人工：按序跑 20260914180000、20260914200000 两个 migration；重新部署 attribution-upload** | supabase/migrations/20260914200000_snapshot_chunked_judge.sql, supabase/functions/attribution-upload/index.ts, supabase/functions/_shared/attribution-report.ts, src/lib/{attributionApi,version}.ts, src/routes/api/public/hooks/attribution-cron.ts, docs/{PLAN,WORKLOG}.md

2026-09-14 | claude | **重算进度显示到前端**：分片判定的进度（已判完哪个站点第几片、剩余/总待判定键数、进度条）接到两个归因页面的等待态里，切 TAB 回来也还在（存在 attributionView 里）。同时修「已等待一直停在 0:00」——原来的秒数完全依赖服务端回调，而回调改成「判完一片才来一次」后间隔可能几十秒，看起来像卡死；改为记录重算开始时间戳、页面每秒本地计算，秒数始终在走，分片进度按回调更新。文案也改成「判定按站点分片执行，判完一片就存库；中途离开本页也不会白跑」。纯前端。版本号见 version.ts。**不需要做任何事，Lovable 同步 main 后自动生效。** | src/routes/{gmv-attribution,gmv-attribution-admin}.tsx, src/lib/{attributionApi,attributionView,version}.ts, docs/WORKLOG.md

2026-09-14 | claude | **修「重算跑完了但页面不自动显示」**：收尾（`stage=finish`）是纯数据库聚合，2026-08 光 `attribution_apply_run` 就 67 秒，加上去重计数/内容类型/报表构建后整个请求会撞网关连接超时；服务端其实还在跑并把快照写完了，但前端拿不到返回值就直接报错、不刷新，于是「跑完了却是空页面」。两处加固：①`refreshAsync` 收尾失败不再直接判死，改为最多轮询 5 分钟，出现属于本次 run 的 READY 快照就当成功（FAILED 才报错）②两个归因页面在 finally 里无条件重新读一次快照，无论前面成没成功——服务端已经写好的结果不该因为前端报错就看不到。版本号见 version.ts。**不需要做任何事，Lovable 同步 main 后自动生效。** | src/lib/{attributionApi,version}.ts, src/routes/{gmv-attribution,gmv-attribution-admin}.tsx, docs/WORKLOG.md

2026-09-15 | claude | ①「导出唯一VID汇总」与「回写飞书进度」按钮右侧加「?」说明：导出的粒度（站点×VID×商品ID）、列顺序、金额口径（归并时已折 USD、ROI/CTR/CVR 用汇总后的分子分母重算）；回写写的是飞书主表格「绩效统计记录」sheet，从第一个空行**追加**、不覆盖历史，A–S 列含义逐列列出（A 回写时间 / B 月份 / C 站点 / D 同事 / E 角色 / F GMV / G 消耗 / H 订单 / I 目标 / J 进度% / K VID匹配GMV / L 昵称匹配GMV / M 商品卡GMV / N 未归因GMV / O–R 预留 / S 离职标记），并提醒同月重复点会追加多份 ②导出新增归属四列（归属人 / 角色 / 归属类别 / 匹配方式），新 RPC `attribution_run_owner_by_vid` 从该月最新快照按 (站点,VID) 取金额最大的那条判定；这样导出表同时有站点、VID、达人昵称和归属人，可与本地 Excel 逐行对账 ③无建联达人表改 20 行/页，默认按**所选月份** GMV 降序（原按 12 个月合计排序，会把早就不投的老达人顶到前面）④**修无建联趋势表的截断**：原来每个月只取前 5000 条无建联明细再聚合，而一个月实际有几万条，长尾被整段砍掉，月度数字偏小且每月截断位置不同导致跨月不可比；改为新 RPC `attribution_run_unmatched_by_creator` 在库内按 (站点,归一化昵称) 全量聚合后返回。**待人工：跑 migration 20260915090000_export_owner_and_unmatched.sql；重新部署 attribution-upload** | supabase/migrations/20260915090000_export_owner_and_unmatched.sql, supabase/functions/attribution-upload/index.ts, src/routes/gmv-attribution-admin.tsx, src/components/attribution/UnmatchedTrendTable.tsx, src/lib/{attributionApi,version}.ts, docs/{PLAN,WORKLOG}.md

2026-09-15 | claude | **修「导出唯一VID汇总」失败（Failed to send a request to the Edge Function）**：这个导出还在用「把整月归并行分页拉进 Edge Function 再用 JS 聚合」的老写法，2026-08 是 25 万组、250 多次往返加 25 万行 JS 聚合，会被超时或 CPU 配额掐断，客户端看到的就是这条网络层报错。新增 migration `20260915120000_vid_summary_sql.sql`：`attribution_vid_summary_json` 一条 SQL 完成「按 (站点,VID,商品ID) 聚合 + 关联 SKU + 带上该月最新快照的归属」并打成 JSON 分页返回，`attribution_vid_summary_count` 给总行数；另加索引 `attribution_run_rows(run_id, country, vid)`，否则 25 万次归属 LATERAL 会退化成全表扫。Edge Function 一次只回一页（两万行，整月一次性返回是几十 MB，扛不住也可能被截断），客户端翻页拼装并与总数对账，数量不符直接报错；导出超过两万行时显示「正在导出 x / y 行」。顺手删掉已无人调用的 streamAgg。**待人工：跑 migration 20260915120000_vid_summary_sql.sql；重新部署 attribution-upload** | supabase/migrations/20260915120000_vid_summary_sql.sql, supabase/functions/attribution-upload/index.ts, src/lib/{attributionApi,version}.ts, src/routes/gmv-attribution-admin.tsx, docs/WORKLOG.md

2026-09-15 | claude | 复核 Lovable 的「导出超时」修复并补两处：①确认他的改法没有削弱完整性——翻页只在「本页行数 < 页大小」时结束，中途任何一页出错都会抛，所以循环正常走完就等于取全了，总行数只是进度分母，缺了不影响正确性 ②但那次 count 仍留在第一页请求里，要整月扫一遍 GROUP BY，等于给第一次请求平白加一次全月聚合、更容易超时；改为**导出流程完全不调用它**（函数保留供单独查询），进度文案改成「已取 N 行」 ③**修达人昵称取值口径**：原 JS 是「把同一 (站点,VID,商品ID) 下每种昵称写法的行数加起来取最多的」，我搬进 SQL 时写成了 `(array_agg(account_name ORDER BY rows_count DESC))[1]`，那是「取单条归并行行数最大的那条的昵称」——归并行按 (批次,内容类型,币种) 拆开，同一昵称散在多条里，必须加总才对。新增 migration `20260915140000_vid_summary_name_fix.sql`，在分页后的结果上按 (站点,VID,商品ID,昵称) 汇总行数再 DISTINCT ON 取最大，代价跟页大小走；顺带给 `ad_upload_agg(month, country, vid, product_id)` 加索引。这张表是拿去和本地 Excel 逐行对账的，昵称口径不能有偏差。**待人工：跑 migration 20260915140000_vid_summary_name_fix.sql；重新部署 attribution-upload** | supabase/migrations/20260915140000_vid_summary_name_fix.sql, supabase/functions/attribution-upload/index.ts, src/lib/{attributionApi,version}.ts, src/routes/gmv-attribution-admin.tsx, docs/WORKLOG.md

2026-09-15 | lovable | 执行 migration `20260915140000_vid_summary_name_fix.sql`：修复「唯一VID汇总」达人昵称取值口径（同一昵称跨批次/内容类型/币种加总后取行数最多），并为 `ad_upload_agg` 补 `(month, country, vid, product_id)` 索引；重新部署 Edge Function `attribution-upload`。版本号 0915.078。**无需额外人工步骤** | supabase/migrations/20260915140000_vid_summary_name_fix.sql, supabase/functions/attribution-upload/index.ts, src/lib/version.ts, docs/WORKLOG.md

2026-09-15 | claude | ①**无建联面板标题与内容对不上**：卡片标题写「按 GMV 降序，共 200 个」，取的是 `report.unmatched.top` 的长度，但卡片里实际渲染的是 `UnmatchedTrendTable`（该月全部无建联达人 + 12 个月趋势，两千多个），两者完全是两份数据——看起来就像被截断到 200。标题改为「无建联达人（供补建联参考）」，条数由表格自己显示。②**修 `attribution_run_unmatched_top` 丢站点**：原来 `GROUP BY lower(btrim(account_name))` 不带站点，PH 的「Eagle」和 MX-AR 的「Eagle」会被并成一行、金额相加、站点信息丢失，拿去补建联根本不知道找哪个站点；归因的身份键一直是「站点 + 归一化昵称」，这里必须对齐。新增 migration `20260915160000_unmatched_top_by_country.sql`，改为按 (站点, 归一化昵称) 分组并返回站点，归一化统一用 `attribution_norm_name`（原 `lower(btrim())` 不做 NFKC、也不把占位符归空）。③**内容类型面板补素材数**：「图片」以及认不出的创意类型都归入「其他」，这类素材通常 GMV=0，只看金额时整列都是 $0、像是数据丢了；现在每格在占比后面附「· N 条」，并在说明里写清图片归入其他、要看素材数才能确认它们确实进来了。**待人工：跑 migration 20260915160000_unmatched_top_by_country.sql；重新部署 attribution-upload** | supabase/migrations/20260915160000_unmatched_top_by_country.sql, supabase/functions/attribution-upload/index.ts, supabase/functions/_shared/attribution-report.ts, src/components/attribution/{ProgressBoard,TypeMixPanel}.tsx, src/lib/{attributionApi,version}.ts, docs/WORKLOG.md
2026-09-15 | lovable | 执行 migration 20260915160000_unmatched_top_by_country.sql（无建联达人 TOP 按站点分统计、归一化口径对齐引擎），并重新部署 attribution-upload Edge Function；版本号改为 0915.080 | supabase/migrations/20260915160000_unmatched_top_by_country.sql, supabase/functions/attribution-upload/index.ts, src/lib/version.ts, docs/WORKLOG.md

2026-09-15 | claude | ①**修无建联达人只剩 JP / MX-AR**：`attribution_run_unmatched_by_creator` 是 RETURNS TABLE，被 PostgREST 的 1000 行上限截断，而该聚合输出按 (站点, 归一化昵称) 有序、站点字母序 JP → MX-AR → … 前 1000 行正好被这两个站点吃满，页面站点下拉就只剩它们。与之前判定键那次是同一类坑。改为 `attribution_run_unmatched_by_creator_json` 打成单行 JSON 分页返回（显式 ORDER BY 保证翻页稳定），行集版本直接 DROP 掉免得以后误用；Edge 端翻页取全。②**导出后半程卡顿**：25 万行先攒成对象数组再转表格等于在浏览器里存两份；改为流式 `vidSummaryStream`，边下边直接写成表格行，页大小 5000 → 20000（请求数 50 → 13），每页之间让出主线程；并把「正在下载 x 行」「正在生成 Excel」两个阶段分开提示，生成阶段前先 await 一次让 toast 渲染出来，否则用户看到的就是「卡住」。③顺带把达人昵称口径修正补进游标分页版 `attribution_vid_summary_page`（按 (站点,VID,商品ID,昵称) 汇总行数后取最多，而不是取单条归并行行数最大的那条）。**待人工：跑 migration 20260915180000_unmatched_by_creator_json.sql；重新部署 attribution-upload** | supabase/migrations/20260915180000_unmatched_by_creator_json.sql, supabase/functions/attribution-upload/index.ts, src/lib/{attributionApi,version}.ts, src/routes/gmv-attribution-admin.tsx, docs/WORKLOG.md

- 0915.082 执行 20260915180000_unmatched_by_creator_json.sql（未建联达人改 JSON 分页返回、VID 汇总昵称口径修正为加总后取最多、补 run_rows 索引）并重新部署 attribution-upload

2026-09-16 | claude | **飞书基础数据每晚自动同步**：新增服务端路由 `/api/public/hooks/feishu-sync-cron` + migration `20260916120000_feishu_sync_cron.sql`（pg_cron job `feishu-sync-nightly`，`0 15 * * *` = 北京 23:00，域名与 apikey 从已有 hook job 命令里抄，不把密钥写进迁移）。一次串行跑四步，等价于人工点四个按钮：①发样及素材统计「同步飞书数据」(`feishu-read-connection-stats`) ②「同步达人登记」(`attribution-sync-creators`) ③「同步 GMV 目标」(`attribution-feishu` / `sync-targets`) ④「同步站点交接」(`attribution-feishu` / `sync-handovers`)。四步互不影响（单步 4 分钟超时，失败继续下一步），结果 upsert 到 `gmv_max_sync_state('feishu_nightly_sync')`，有失败时发飞书机器人通知（含 VID 精度丢失 / 没读到 VID 这类静默失准告警）。刻意排在归因快照（北京 23:30）之前半小时——快照按当下的登记数据现算，先同步完当晚快照才吃得到当天新数据。cron 免口令抽成 `_shared/cron.ts` 的 `cronAuthed(req)`（`x-cron-key` → `verify_gmv_cron_key` RPC），三个函数接入：`feishu-read-connection-stats`、`attribution-sync-creators` 全量放行（两者只读飞书、只写自己库），`attribution-feishu` 仅放行 `sync-targets` / `sync-handovers`，回写飞书与人工判定类 action 仍必须带管理口令。前端三处说明文案标注「每晚北京 23:00 自动同步一次，按钮只在要马上拿最新数据时用」。版本号 0916.084。**待人工：跑 migration `20260916120000_feishu_sync_cron.sql`；重新部署 `feishu-read-connection-stats`、`attribution-sync-creators`、`attribution-feishu` 三个 Edge Function** | src/routes/api/public/hooks/feishu-sync-cron.ts, supabase/migrations/20260916120000_feishu_sync_cron.sql, supabase/functions/_shared/cron.ts, supabase/functions/{feishu-read-connection-stats,attribution-sync-creators,attribution-feishu}/index.ts, src/routes/{connection-stats,gmv-attribution-admin}.tsx, src/lib/version.ts, docs/{ARCHITECTURE,PLAN,WORKLOG}.md

2026-09-16 | claude | **GMV 归因 V3 阶段 1+2**（按 `GMV_ATTRIBUTION_V3_PLAN` v3.0，**本次不改变任何归因数字**）。阶段 1：给 `_shared/attribution.ts` 的纯函数补 35 个 `Deno.test` 用例，固化的是**现状**而非 V3 目标行为，与规范不一致处（3 个自然月保护期 vs 90 天、`pickVidOwner` 按姓名去重的双角色 bug、VID 反推发布时间兜底、别名直接定 BD）全部用 `// V3:` 标注将来要改成什么；不引 vitest/jest，也不引 std/assert（deno.land 在云端会话不可达），断言写在测试文件里。阶段 2：①事实层 5 张新表 + `site_handovers` 补 `scope`/`creator_keys`，导入计划 §7.1 的站点权限 14 行（结束日期按半开区间换算：「2026-06-30 结束」→ end_date 2026-07-01）与 §7.2 的 2 条真交接；②身份三层表（边=事实层 append-only / `creator_entities`=半持久，creator_id 永不重新随机生成 / 别名=派生层）+ `creator_attribution_stages`（daterange + EXCLUDE gist 防重叠）；③纯逻辑落在 `_shared/identity.ts`（并查集算身份分量、稳定分配 creator_id、字段级最新值空不覆盖非空）与 `_shared/stages.ts`（一条登记行展开成发样/回收两个事件、90 自然天 `[D-90,D)`、交接 > 人工判定 > 90 天的事件优先级），27 个用例按计划 §九测试矩阵编号；④新 Edge Function `attribution-identity-build`（build/report）全量生成并出报告，前端「GMV 归因·管理」新增「达人身份层 · 归属区间预演」面板；⑤对账视图 `creator_stage_ownership_diff` 与 `attribution_posted_at_gap`（计划 §7.5 的阶段 0 SQL 做成视图）；⑥`attribution-feishu` 的 `sync-handovers` 按计划 §7.2 过滤「原BD=无」的初始分配行（否则引擎会去找一个名叫「无」的 BD），并在返回里单列 `initial_assignments`。两份 migration 已在本地 PostgreSQL 16 实跑：区间重叠、同人同站点权限区间重叠、「都不算」规则形状约束都按预期被拒绝，首尾相接的区间正常插入，两个视图与 RPC 可查。前端 tsc 未能运行（本环境 npm registry 不可达，node_modules 装不上），改动只用了已有 UI 组件与 `invokeFn`。版本号 0916.086。**待人工：跑 migration `20260916140000_attribution_v3_fact_tables.sql`、`20260916140100_attribution_v3_identity_stages.sql`；部署新 Edge Function `attribution-identity-build`；重新部署 `attribution-feishu`** | supabase/migrations/20260916140000_attribution_v3_fact_tables.sql, supabase/migrations/20260916140100_attribution_v3_identity_stages.sql, supabase/functions/_shared/{identity,stages,attribution.test,identity.test}.ts, supabase/functions/attribution-identity-build/index.ts, supabase/functions/attribution-feishu/index.ts, src/components/attribution/IdentityPreviewPanel.tsx, src/lib/{attributionApi,version}.ts, src/routes/gmv-attribution-admin.tsx, docs/{ARCHITECTURE,PLAN,WORKLOG}.md

2026-09-16 | claude | 部署新 Edge Function `attribution-identity-build`，并重新部署 `attribution-feishu`（GMV 归因 V3 阶段 1+2 的待人工部署项）；版本号 0916.087 | supabase/functions/{attribution-identity-build,attribution-feishu}/index.ts, src/lib/version.ts, docs/WORKLOG.md

2026-09-16 | claude | GMV 归因 V3 阶段 0 的两项确认落文档：①VN 站点林丽洪权限起始日 2024-01-01 已确认，不再是待确认项（migration 已执行，按 AGENTS.md 不改历史 migration，DB 里那行备注的「待确认」字样留作历史，可用一句 UPDATE 改文案）；②`attribution_posted_at_gap` 跑出缺「发布时间」GMV 占比约 15%，超过计划 §7.5 的 5% 阈值，但该口径包含了被 VID 强归因命中的行与商品卡片（这两类根本不需要发布时间），真实会进 PENDING 的占比要另算，结论出来前阶段 3.1（posted_at 改必填 + 弃用 VID 反推发布时间）先不动。版本号 0916.087。**不需要做任何事，Lovable 同步 main 后自动生效** | docs/PLAN.md, docs/WORKLOG.md, src/lib/version.ts

2026-09-16 | claude | **保护期统一 90 自然天**（计划 §八 3.3 提前做，**会改归因数字**）。①全项目扫了一遍「3 个月」：保护期逻辑**只存在于 `_shared/attribution.ts` 的 `resolveOwnership` 一处**（SQL 侧只把 creator_ownership 当查找表，从没实现过判定），改成 `diffDays(ownerLast, e.date) >= 90`；`PROTECTION_DAYS` / `addDaysISO` / `diffDays` 统一放在 attribution.ts，stages.ts 原样再导出，保证区间层与线上引擎永远同口径。旧的 `addMonthsISO` 已删。管理页「3 个月保护期」文案同步改成 90 天并补了窗口说明。②**发布时间口径修正**：直播与商品卡片的广告行本身没有「发布时间」，之前 `attribution_posted_at_gap` 把它们算进缺失，导致占比虚高（之前看到的 15% 含这部分）。新增 `attribution_needs_posted_at()`，视图重建：分母只算应该有发布时间的金额，直播/商品卡单列 `no_posted_at_by_design_usd`，另加 `pending_gmv_usd`（应有却为空且未被 VID 强归因命中）= 阶段 3.1 之后真正会进 PENDING 的金额。连带明确：阶段 3.1 的「posted_at 必填」指**表头必须有这一列**，不是每行都有值。③**一致性校验**：新增独立的 SQL 第二实现 `attribution_protection_owner_90d(站点, 天数)` 与对账视图 `attribution_protection_check_90d`（引擎落库 vs SQL 重算，正常应零差异）。本地 PostgreSQL 16 用 5 个边界用例（第 90 天转移 / 第 89 天抢注 / 2月1日→5月1日只有 89 天 / 同 BD 刷新后 45 天抢注 / owner 无日期直接转移）与 TS 引擎逐行比对，owner、首次日期、最近动作日、转移与抢注计数全部一致；再人为改错一行与删掉一行，视图都按预期报出 OWNER_DIFFERS / ONLY_IN_SQL。测试 63 例全绿。版本号 0916.088。**待人工：跑 migration `20260916160000_protection_90d_consistency.sql`；重新部署 `attribution-sync-creators`、`attribution-run`、`attribution-upload`、`attribution-feishu`、`attribution-identity-build`；部署后到管理页点一次「同步达人登记」，让归属按 90 天重算** | supabase/functions/_shared/{attribution,stages,attribution.test}.ts, supabase/migrations/20260916160000_protection_90d_consistency.sql, src/routes/gmv-attribution-admin.tsx, src/lib/version.ts, docs/{ARCHITECTURE,PLAN,WORKLOG}.md

2026-09-17 | claude | 重新部署 5 个依赖 _shared/attribution.ts 的 Edge Function（attribution-sync-creators / attribution-run / attribution-upload / attribution-feishu / attribution-identity-build），使保护期 90 自然天改动在运行时生效；版本号 0916.089 | supabase/functions/{attribution-sync-creators,attribution-run,attribution-upload,attribution-feishu,attribution-identity-build}/index.ts, src/lib/version.ts, docs/WORKLOG.md

2026-09-17 | claude | 「同步达人登记」按钮的说明文案重写：原来只有三行泛泛的「读飞书三处登记」，现在逐表逐列写清楚 —— 建联表（主表格「建联-姓名」sheet，A2:Z）的 B 发样日期 / C 地区店铺 / D 用户名 / E 昵称 / K SKU / N 登记日期 / P VID，粉丝量列按表头自动找列位，BD 按 sheet 名认不看 A 列；「授权记录」sheet（同表格 M3:S）的 M BD / N 登记日期 / O 国家 / P 达人名字 / Q VID / S SKU，并注明这张表没有用户名和发样日期、归档行只能靠昵称与 VID 归因；剪辑表（FEISHU_EDITOR_SPREADSHEET_TOKEN，A2:H）的 B 同事 / C 日期 / D 国家 / E 账号 / F SKU / G VID，B 列同事与 sheet 名不符的行直接跳过、没有 VID 的行跳过。另补上归属判定口径（90 天保护期、KEYTYPE_CONFLICT 昵称优先）与两个静默失准提醒（站点必须英文简写、VID 列必须文本格式）。版本号 0917.090。**不需要做任何事，Lovable 同步 main 后自动生效**（纯前端文案） | src/routes/gmv-attribution-admin.tsx, src/lib/version.ts, docs/WORKLOG.md

2026-09-17 | claude | **修「同步达人登记」超时**。Edge Function 日志里没有任何 error，只有 booted → shutdown，两次间隔都正好 150 秒 = Supabase 墙钟上限，函数是被平台掐断的（所以没有 JSON 错误体，前端只看到「非 2XX」）。登记数据已到 6 万行（JIANLIAN 29,254 / ARCHIVE 21,153 / EDITOR 10,249），「读十来个飞书 sheet + 全量重建登记表 + 解析归属 + 重建归属表」一次做不完。改成按 sheet 分片：时间预算 110 秒，每处理完一个 sheet 检查一次，每片**先删后插自己那个 source_sheet 的行**（粒度安全，中断不会留下半张表），预算用完返回 `done:false` + `next`（剩余 sheet 或 resolve_only），前端 `syncCreators` 与 cron 路由都改成循环续跑到 `done:true`，前端每批弹一次进度提示。归属解析改成**从数据库读全部 BD 登记行**（分片后内存里只剩最后一片），并要求剩余时间 ≥55 秒才开始，不够就让调用方再调一次 `resolve_only`。另新增两张只读盘点视图 `attribution_creator_audit`（每个站点×达人：谁登记过/几行/最早最近动作日/当前归属/占位日期行数）与 `attribution_vid_audit`（每个 VID：登记人/角色/日期/来源表与行号，含 staff_vid_map），用于和飞书历史表逐行比对漏同步。版本号 0917.091。**待人工：跑 migration `20260917120000_registry_audit_views.sql`；重新部署 `attribution-sync-creators`** | supabase/functions/attribution-sync-creators/index.ts, supabase/migrations/20260917120000_registry_audit_views.sql, src/lib/attributionApi.ts, src/routes/gmv-attribution-admin.tsx, src/routes/api/public/hooks/feishu-sync-cron.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md

2026-09-17 | claude | 重新部署 Edge Function `attribution-sync-creators`（按 sheet 分片续跑修 150 秒墙钟超时），版本号 0917.092 | supabase/functions/attribution-sync-creators/index.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md
- 2026-09-17 | claude | attribution-sync-creators 增加数据库网关抖动（521/5xx/HTML 错误页）自动重试，重新部署，版本号 0917.093

2026-09-18 | lovable | 重新部署两个 Edge Function：①`attribution-feishu` 新增 action `bulk-judgment`——审查 Excel 批量导入落库（不碰飞书、在取 tenant token 之前分流；JUDGE→attribution_review+creator_alias(MANUAL)，OVERRIDE_VID→attribution_manual_rules，OVERRIDE_CREATOR→attribution_manual_decisions，IGNORE 只标已处理；先 dry_run 整批校验，有错整批不写并按行号报错）②`attribution-sync-creators` registry_rows 口径修正。版本号 0918.095。**不需要做任何事，Lovable 同步 main 后自动生效** | supabase/functions/{attribution-feishu,attribution-sync-creators}/index.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md

2026-09-17 | claude | **审查项 Excel 批量处理 + 人工判定/人工覆盖分流**。①`attribution-feishu` 新增 `bulk-judgment` action，按行的 decision_type 分流落库：JUDGE → attribution_review.manual_bd（昵称类顺带 creator_alias MANUAL，仍在归因瀑布原位置生效）；OVERRIDE_VID → attribution_manual_rules（VID 级、**最高优先级**、永久有效可停用）；OVERRIDE_CREATOR → attribution_manual_decisions（达人级，从生效日建立归属阶段）；IGNORE 只标已处理。该 action 完全不碰飞书，代码放在取飞书 token 之前 —— 飞书挂了不该连判定都做不了；批量也不逐行回写飞书（几百行必然超时），判定以数据库为准。导入前强制 dry_run：姓名不在人员表、VID 位数不对、达人覆盖缺站点/昵称、原因没填，都会被逐行报出来，**有一行错就整批不写**（填表错误往往成片出现，写一半更难收拾）。②前端新增 `ReviewExcelPanel`，挂在「审查与回写」面板顶部：导出当前筛选结果为 xlsx（sheet1 审查项、sheet2 可选值与填表说明），填完传回来自动校验+落库。**下拉的限制说清楚了**：SheetJS 社区版不支持写 Excel 数据验证，所以模板里没有原生下拉，改用「可选值 sheet + 导入强校验」，并在说明里写了自己设数据验证的方法。③顺手修两处口径：同步返回的 registry_rows 改成整张登记表的 count（分片后内存里只剩最后一片，之前报的是 BD 行数）；`adExcel` 的「发布时间」列加模糊兜底识别（「视频发布时间」「发布日期」「Post time」这类列名精确匹配漏掉会让整月发布时间全空）。版本号 0917.094。**待人工：重新部署 `attribution-feishu` 与 `attribution-sync-creators`** | supabase/functions/attribution-feishu/index.ts, supabase/functions/attribution-sync-creators/index.ts, src/components/attribution/{ReviewExcelPanel,ReviewPanel}.tsx, src/lib/{attributionApi,adExcel,version}.ts, src/routes/gmv-attribution-admin.tsx, docs/{PLAN,WORKLOG}.md

2026-09-18 | claude | **修「缺发布时间」统计把直播行误判成缺失**。VN 2026-05 重传后行级确认 33,064 行里 31,089 行有发布时间（图片 233/233 全有），但 `attribution_posted_at_gap` 仍报 74.71% 缺失、10,751 USD —— 行数少、金额占比高，是直播行的特征。根因：`attribution_needs_posted_at` 与 `src/lib/adExcel.ts` 的 `normCreativeType` **两边都只做精确匹配**，「直播带货」「LIVE 带货」「商品卡片推广」这类写法既没被归一成 live / product_card，又没被排除，于是被当成「应该有发布时间却没有」。①SQL 函数改成包含式匹配（含 live/直播 → 直播；含 商品卡/product card/product_card/card → 商品卡片），已用 15 种写法在本地 PostgreSQL 16 逐个验证分类正确；②`normCreativeType` 同步加包含式兜底，与归因引擎 `normalizeCreativeType` 对齐，以后入库就是干净的 video/live/product_card（认不出来的仍保留原文，归因引擎归入「其他」桶）；③新增只读视图 `attribution_creative_type_audit`，列出库里全部内容类型写法、是否被判定为需要发布时间、以及哪些是归一化没认出来的原文，便于发现 TikTok 改写法。这个函数只影响统计口径、不参与归因判定，改它不会动任何 GMV 归属。版本号 0918.096。**待人工：跑 migration `20260918100000_needs_posted_at_fuzzy.sql`**（纯前端的 normCreativeType 改动只影响此后新上传的文件） | supabase/migrations/20260918100000_needs_posted_at_fuzzy.sql, src/lib/{adExcel,version}.ts, docs/WORKLOG.md

2026-09-18 | claude | **撤回上一条的包含式匹配，恢复精确匹配**。上一条是按错误假设改的：当时以为 VN 2026-05 那 10,751 USD「缺发布时间」是直播写法没被认出来，实际那个文件根本没有直播行。真实原因与内容类型无关 —— 那份 gap 统计跑在文件重传**之前**：`ad_uploads.created_at` 是 UTC（09:13+00 = 北京 17:13），而导出文件名是本地时间（16:48 = UTC 08:48），查询比上传早约 25 分钟，数字是旧批次的残影。内容类型是 TikTok 导出的受控取值（视频/商品卡片/直播及英文对应），对受控取值做模糊匹配是负收益：猜中一次不存在的写法，代价是真出现新类型时被静默归进已知类型、再也暴露不出来；认不出来的应该显式落进「其他」让人看见。`attribution_needs_posted_at` 与 `normCreativeType` 都已恢复精确匹配（新增 migration 覆盖回去，不改历史 migration）。保留 `attribution_creative_type_audit` 诊断视图，正好用来盯「库里出现了归一化不认识的写法」。版本号 0918.097。**待人工：跑 migration `20260918110000_needs_posted_at_exact_again.sql`**（若 `20260918100000` 已经跑过，这份会把它覆盖回来；两份都没跑也可以只跑这一份 —— 但那样就没有 `attribution_creative_type_audit` 视图，需要的话两份按顺序都跑） | supabase/migrations/20260918110000_needs_posted_at_exact_again.sql, src/lib/{adExcel,version}.ts, docs/WORKLOG.md

2026-09-18 | claude | 重新部署 Edge Function `attribution-feishu`（bulk-judgment，审查 Excel 批量落库）与 `attribution-sync-creators`（registry_rows 口径修正），线上返回 401 鉴权拦截正常；版本号 0918.098 | supabase/functions/{attribution-feishu,attribution-sync-creators}/index.ts, src/lib/version.ts, docs/WORKLOG.md

2026-09-18 | claude | 修复 `src/routes/gmv-attribution-admin.tsx` 当前构建报错：`ReviewPanel` 所在 `UploadResultView` 作用域里没有 `report` 变量，应使用 `result?.summary?.staff` 取人员名；顺手修正后类型检查通过；版本号 0918.099 | src/routes/gmv-attribution-admin.tsx, src/lib/version.ts, docs/WORKLOG.md

2026-09-18 | claude | **内容类型归一化全部收成精确匹配（三份实现对齐）**。内容类型是 TikTok 导出的受控取值，只有「视频 / 商品卡片 / 直播」及英文写法；对受控取值做包含式兜底是负收益 —— 猜中一次不存在的写法，代价是真出现新类型时被静默归进已知类型、再也暴露不出来。项目里这件事有**三份实现**，之前口径不一致：①`src/lib/adExcel.ts` 的 `normCreativeType`（上传解析，已恢复精确）；②`supabase/functions/_shared/attribution.ts` 的 `normalizeCreativeType`（归因引擎，删掉 %CARD%/%LIVE%/%VIDEO% 三条包含式兜底）；③SQL 的 `attribution_norm_creative_type`（**参与判定** —— `attribution_month_keys_ext` 用它筛候选键，商品卡/直播/其他在别名投票前就分流走），同样删掉三条包含式兜底。认不出来的一律落 'other'（不归人但金额仍在总额里），在「内容类型」面板看得见。那份按错误假设写的包含式 migration（20260918100000）已从仓库删除，诊断视图 `attribution_creative_type_audit` 并入 20260918110000（先 DROP 再建，跑过旧版的库也能覆盖）。③的改动**可能改数字**：只影响写法不是受控取值的行，改前可先查 `SELECT * FROM attribution_creative_type_audit WHERE is_raw_value;` 确认影响面，没有行就是零影响。版本号 0918.100。**待人工：跑 migration `20260918110000_needs_posted_at_exact_again.sql`；重新部署 `attribution-run`、`attribution-upload`、`attribution-feishu`** | supabase/migrations/20260918110000_needs_posted_at_exact_again.sql, supabase/functions/_shared/{attribution,attribution.test}.ts, src/lib/{adExcel,version}.ts, docs/WORKLOG.md

2026-09-18 | claude | **归因审查页重做 + 新增归因查询**。①`list-reviews` 去掉 500 条上限（截断会让人以为「只有这些」而漏判），改成分页拉全量，前端每页 10 条并加类型筛选；②审查卡片重做：顶上一句话点明**冲突维度**（VID / 昵称↔用户名 / 保护期抢注 / 别名证据），下面统一成「候选归属 | 依据 | 站点 | 首次登记 | 最近登记 | 来源证据」表格 —— 之前 `KEYTYPE_CONFLICT` 根本没有渲染分支，直接把 JSON 吐在卡片里（用户截图里那张就是），而且 detail 里既没有国家也没有日期，人无从判起；现在 `attribution-sync-creators` 在生成该审查项时补上 country 与昵称侧/用户名侧各自的首次、最近建联日期；③判定输入从 datalist 改成真下拉：**本条冲突的候选人 + 「忽略」排最前**，其余同事放 optgroup，名单来自新 action `list-staff`（含离职，历史冲突可能要判给离职同事）—— 之前 datalist 在多数浏览器里不点不出内容，等于没有下拉；④「归因结果」tab 之前没上传就完全空白，现在常驻**归因查询**：输入 VID（15–20 位精确）或达人昵称（模糊），返回三块 —— 每月归给了谁（只取每月最新快照，否则重算过的月份会重复出行）、登记记录（谁在哪张表第几行登记过）、当前归属；每页 10 行，走新 RPC `attribution_lookup` 返回单个 JSON，避开 PostgREST 对 RETURNS TABLE 的 1000 行静默截断。版本号 0918.101。**待人工：跑 migration `20260918140000_attribution_lookup.sql`；重新部署 `attribution-feishu`、`attribution-sync-creators`、`attribution-upload`；部署后点一次「同步达人登记」让 KEYTYPE 冲突带上日期/国家** | src/components/attribution/{ReviewPanel,AttributionLookupPanel}.tsx, src/lib/{attributionApi,version}.ts, src/routes/gmv-attribution-admin.tsx, supabase/functions/{attribution-feishu,attribution-sync-creators,attribution-upload}/index.ts, supabase/migrations/20260918140000_attribution_lookup.sql, docs/{PLAN,WORKLOG}.md

2026-09-18 | claude | **归因兜底逻辑按清单逐条收紧（A4 / A5 / A7 / B2 / B7，会改数字）**。①**A4 无日期不归因**：`resolveOwnership` 里无日期的登记行以前按 '0000-00-00' 排最前，等于「没填日期的人最早建联」凭空抢归属，而且 owner 无日期时无法主张保护期、后面任何人一登记就转移；现在直接跳过，被跳过的行记进 evidence.skippedNoDate 方便回飞书查。SQL 第二实现 `attribution_protection_owner_90d` 同步（新 migration，加 `IS NOT NULL` 过滤、去掉 NULLS FIRST 与「owner 无日期直接转移」分支），两边口径必须一致否则一致性视图会报假差异。②**A5 VID 双登记不再默认取较新者**：改成「登记日期最新的人是该站点当前负责人、其余候选都不是」才自动归他（规则来源 `staff_site_permissions`，判定基准日 `asOfDate` 默认今天、重跑历史可由调用方传当月月末）；其余情况**不猜** —— VID 这一层直接放弃，该行落到达人昵称路径，同时产 VID_DUAL_SOURCE 审查项等人判，人判完经 reviewOverrides 立即生效。旧行为是在没有依据时替人做决定，数字看着完整但有一半可能是错的且没人知道。③**A7 禁止 VID 反推发布时间**：来源顺序固定为「GMV MAX 导出列 → 飞书素材登记日期」，登记日期按两条线取最早（该 VID 的登记行 / 该达人昵称的全部登记动作 —— 昵称路径上多数行没有 VID 登记，只有后者给得出日期）；`attribution-upload` 两处与 `attribution-report` 一处的 `vidToPostedAt` 调用全部删除，`postedAtSource` 增加 `registry`。VID 反推算出来是 UTC，当站点当地日期用，MX（UTC-6）与东南亚（UTC+7/8）必然跨日误判。④**B2 删除**发布时间列的模糊表头兜底（与内容类型同理：列名是受控输出，认不出来应该在「未识别表头」诊断里暴露）。⑤**B7** `vidToPostedAt` 合理区间由 2008–2100 收窄到 2022–2030，且该函数只剩交接边界抽查提示在用。测试 69 例全绿（新增 A4 两例、A5 四例、A7 两例、B7 一例）。版本号 0918.103。**待人工：跑 migration `20260918160000_protection_no_date_rows.sql`；重新部署 `attribution-run`、`attribution-upload`、`attribution-feishu`、`attribution-identity-build`** | supabase/functions/_shared/{attribution,attribution-report,attribution.test}.ts, supabase/functions/attribution-upload/index.ts, supabase/migrations/20260918160000_protection_no_date_rows.sql, src/lib/{adExcel,version}.ts, docs/WORKLOG.md

2026-09-18 | claude | **C1：飞书表名称配置化，去掉 sheet 别名兜底**。原来 `attribution-feishu` 里有一张 `SHEET_ALIASES`（「归因进度」也认「绩效统计」「绩效记录」等），飞书改名就靠别名去猜 —— 猜对一次的代价是真改名时静默读到另一张表、或者读空还以为「本期没数据」。现在：①新表 `feishu_sheet_config`，字段为 飞书表格名称 / token 来源 secret / sheet 名称 / 读取列范围 / 读写(READ|READWRITE) / 说明备注 / 列映射(jsonb) / 启用 / 排序，初始 8 行就是现在硬编码的全量（建联表、授权记录、剪辑表、绩效统计记录、归因审查、归因记录、绩效配置表、发样及素材统计），列映射逐列写明「哪一列 → 哪个字段 → 含义与坑」（站点必须英文简写、VID 列必须文本格式等）；②代码改成从配置读 sheet 名、**只做去空白后精确匹配**，对不上直接报错并列出该表格现有的 sheet，提示去设置页改；配置表还没跑 migration 时退回代码里的默认名，不至于全挂；③设置页新增「飞书表名称」子 tab（管理员可见）：表格名称/sheet 名称/读取范围/说明可直接编辑保存，**改完下次同步立即生效，不用改代码也不用重新部署**；读写列用红色徽标标出「系统会写回去」的表；每行一个「映射」按钮，点开才展开列级说明（列映射只读 —— 改列位必须同时改解析代码）。版本号 0918.104。**待人工：跑 migration `20260918170000_feishu_sheet_config.sql`；重新部署 `attribution-feishu`** | supabase/migrations/20260918170000_feishu_sheet_config.sql, supabase/functions/attribution-feishu/index.ts, src/components/settings/FeishuSheetConfigPanel.tsx, src/routes/settings.tsx, src/lib/{attributionApi,version}.ts, docs/{PLAN,WORKLOG}.md

2026-09-18 | lovable | 重新部署 Edge Function：attribution-feishu、attribution-sync-creators、attribution-upload、attribution-run、attribution-identity-build（list-reviews 去上限/list-staff/bulk-judgment/飞书表名配置/sheet 精确匹配，KEYTYPE 冲突补国家与日期，A4/A5/A7 引擎规则，新增 lookup 查询）。版本号 0918.105。 | supabase/functions/*, src/lib/version.ts, docs/WORKLOG.md

2026-09-18 | claude | **B6 parseDate 删掉第四层兜底 + 飞书表名配置表补齐与改版**。①**B6**：`_shared/cells.ts` 与 `feishu-read/index.ts` 的 `parseDate` 原来最后一层是「把 `.` `/` 换成 `-` 再交给 `new Date()`」—— 这层对 `03-04-2024` 这类写法会自己猜月/日顺序，猜错时返回的是一个**看起来完全正常**的日期，没有报错没有告警，保护期与归属转移直接按错的日子算。现在只保留「Excel 序列号」与「年在前的精确写法（`-` `/` `.` `年月日`，可带时间）」两类，其余一律 `null`，由 A4「无日期的登记行不参与归属判定」接住并记进 `evidence.skippedNoDate`，在审查里看得到。新增 `_shared/cells.test.ts` 6 例钉死该行为（含 `03-04-2024`/`Jan 5, 2024` 必须为 null），`_shared/` 全量测试 75 例全绿。②**C 类配置表补齐**：原来只收了归因链路上的 8 张，漏了授权流程与 SKU —— 配置表漏一处就等于不能信，出问题还得回去翻代码。新增 4 行：`AUTH_READ`（建联 sheet A2:W，授权流程读，且 **feishu-writeback 会回写 V=投放日期 / W=状态**）、`AUTH_LOG`（授权记录 A2:I 追加，与 ARCHIVE 同一张 sheet 但不同列段不同方向）、`SKU`（SKU匹配表 A2:F）、`MANUAL_OVERRIDE`（**名称留空 = 还没提供给系统**，占位停用；人工覆盖目前走 Excel 导入不读飞书）；同一张 sheet 按「函数用途」分行不去重（建联-{姓名} 有四个入口，列范围与读写方向都不同，合成一行反而看不出授权流程会写回去），`CONNECTION_STATS` 排序挪到 18 与另两个建联入口相邻；JIANLIAN/EDITOR 备注补上「feishu-read-bd-vids / feishu-read-editors 也在读」。③**设置页改版对齐人员表**：平时纯展示表格，点右侧铅笔才进编辑，底部保存/取消；**读取列范围改为只读**（写死在解析代码里，界面上改只会让配置说的和系统做的不一致），新增 **key 列**（code-key，方便指名道姓说「改 AUTH_LOG 这行」），按飞书表格名称分组排序、同表格的 sheet 挨在一起、留空的排最后并标「待提供」，说明备注改成整段换行显示（编辑态用 Textarea）；后端 `save-sheet-config` 相应去掉 `read_range` 可写、允许 sheet 名留空并对留空行强制停用（空名字去精确匹配只会报「没有这张 sheet」，不如不参与）。版本号 0918.106。**待人工：跑 migration `20260918180000_feishu_sheet_config_more.sql`** | supabase/functions/_shared/{cells.ts,cells.test.ts}, supabase/functions/{attribution-feishu,feishu-read}/index.ts, supabase/migrations/20260918180000_feishu_sheet_config_more.sql, src/components/settings/FeishuSheetConfigPanel.tsx, src/lib/version.ts, docs/{PLAN,WORKLOG}.md

2026-09-18 | lovable | 按用户要求重新部署 6 个 Edge Function：attribution-feishu、feishu-read、attribution-sync-creators、attribution-upload、attribution-run、attribution-identity-build（main 分支当前代码，无业务逻辑改动）；版本号改为 0918.107 | supabase/functions/{attribution-feishu,feishu-read,attribution-sync-creators,attribution-upload,attribution-run,attribution-identity-build}/index.ts, src/lib/version.ts, docs/{PLAN,WORKLOG}.md

2026-09-18 | claude | **attribution-sync-creators / feishu-read-connection-stats 接上飞书表名配置，sheet 解析收敛成一份**。此前这两个函数的 sheet 名还硬编码，等于「设置 → 飞书表名称」改了对它们不生效 —— 配置表只要有一处不管用，这张表就不能信，出问题还是得回去翻代码。①新增 `_shared/sheetConfig.ts` 作为唯一解析入口（`loadSheetConfig` / `configuredSheetName` / `isSheetEnabled` / `normTitle` / `makeSheetResolver` / `makeOptionalResolver`），`attribution-feishu` 里那份本地实现删掉改为引用，三个函数从此同一套行为：只归一空白后**精确匹配**，对不上就抛错并列出该表格现有 sheet + 指路设置页。以前同一件事（飞书改了表名）在不同函数里有三种表现，最贵的两种是静默读空当「本期没数据」和靠别名猜到另一张表上去 —— 都不报错，数字照出，错得看不出来。②**配置能控制什么，如实说清楚**：固定名字的 sheet（授权记录 / 绩效配置表 / 归因审查 / 归因记录）名称由配置表给，改名改配置即可；`建联-{同事姓名}`、`{剪辑姓名}` 这类**每人一张**的 sheet 真实名称来自「人员表」，配置表里存的是带占位符的模板，只说明这类表读哪些列、不参与匹配，前端对这类行的 sheet 名称禁用编辑并标注「每人一张，名称取自人员表」。③配置表的 `enabled` 现在真的有用：停用 JIANLIAN / ARCHIVE / EDITOR / CONNECTION_STATS 会让对应数据源整个不读，而不是读回一堆空行；被停用的源在响应里以 `disabled_sources` 如实返回，免得「数字少了一截」查不到原因。④`授权记录` 归档 sheet 读不到改为**抛错**而不是记进 missing 继续跑：它是固定名字，读不到就是配置不对，静默少读两万行历史登记会让归属大面积改判且没人知道（每人一张的 sheet 仍记 missing，离职同事的 sheet 可能真被删了）。⑤新增 `orphan_sheets` 盘点：库里还留着、但已不属于任何数据源的 `source_sheet`（多半是 sheet 改过名）。登记行按 source_sheet 先删后插，改名后旧名字那批行没人再删，同一批登记存两份、保护期会当成「两个人各登记过一次」来算 —— 这是改表名后最难自己发现的脏数据。**只报告不自动删**：删的是六万行量级的历史登记，删错无法回滚，交给人看过再决定。新增 `_shared/sheetConfig.test.ts` 8 例（配置读不到要退回默认名、空白差异能匹配但别名不能、报错必须列出现有 sheet 并指路设置页），`_shared/` 全量 83 例全绿。版本号 0918.108。**待人工：无新增 migration；重新部署 `attribution-sync-creators`、`feishu-read-connection-stats`、`attribution-feishu`** | supabase/functions/_shared/{sheetConfig.ts,sheetConfig.test.ts}, supabase/functions/{attribution-sync-creators,feishu-read-connection-stats,attribution-feishu}/index.ts, src/components/settings/FeishuSheetConfigPanel.tsx, src/lib/version.ts, docs/{PLAN,WORKLOG}.md

2026-09-18 | claude | **sheet 名模板化 + 改名自动迁移数据（取代「盘点孤儿让人去删」）+ 已跑 migration 台账**。①**人员表 sheet名 留空 = 用模板生成**：模板取自「设置 → 飞书表名称」（`建联-{同事姓名}` / `{剪辑姓名}`），占位符换成姓名；填了值 = 该人单独指定，模板管不着。飞书整批改名（建联-X → 建联表-X）从此只改配置里一行，不用在人员表逐人改；名字不规范的人照旧单独填。新增 `staffSheetName()`，`staff-sheets` 的 list 额外返回 `resolved_sheet_name`（前端与 feishu-read 直接用后端算好的名字，前端自己再算一遍迟早不一致、不一致就是读错表且不报错）；`attribution-sync-creators`、`feishu-read-connection-stats`、`feishu-read-bd-vids`、`feishu-read-editors` 全部改用同一份解析；两样都给不出名字的同事记进 `unnamed_staff` 汇报，不静默跳过。BD 建联 sheet 的模板**只认 JIANLIAN 一行**（CONNECTION_STATS 读同一批 sheet，两行各有模板会导致改一个漏一个、两函数读不同表且都不报错）。②**改 sheet 名时自动迁移历史数据**（新增 `_shared/sheetRename.ts`）：人员表或配置表里 sheet 名一变，就把 `creator_registry` / `connection_material_registry` 里旧 `source_sheet` **UPDATE** 成新名字。上一版让同步盘点孤儿行、再让人拿 DELETE 去清，方向是错的——改名是正常运维动作，不该产出「待人工清理」的垃圾，更不该让人对六万行量级的历史登记执行删除（本次就因为示例 SQL 里用了真实 sheet 名 `建联-阿南` 导致 1512 行被误删，虽可由同步从飞书重建，但这类操作本就不该出现在正常流程里）。`orphan_sheets` 保留为只读盘点，正常改名已不会再产生孤儿；剩余场景（人员表删人、数据源停用）属于业务决定，代码只报告不删。③**新增 `docs/MIGRATIONS.md` 已执行 migration 台账**：用户确认跑过的 SQL 在此追加一行（执行日期 / 文件名 / 引入提交 / 干了什么）并从 `supabase/migrations/` 删除文件，文件删了仍可用 `git show <提交号>:<路径>` 取回原文。本次登记并删除已跑的 8 个文件。新增 6 例模板测试（含「带 g 的正则不能用来 test」这条——`isSheetTemplate` 一度用带 g 的正则导致连测两次结果相反），`_shared/` 全量 89 例全绿。版本号 0918.110。**待人工：无新增 migration；重新部署 `staff-sheets`、`attribution-sync-creators`、`feishu-read-connection-stats`、`feishu-read-bd-vids`、`feishu-read-editors`、`attribution-feishu`** | supabase/functions/_shared/{sheetConfig.ts,sheetConfig.test.ts,sheetRename.ts}, supabase/functions/{staff-sheets,attribution-sync-creators,feishu-read-connection-stats,feishu-read-bd-vids,feishu-read-editors,attribution-feishu}/index.ts, src/components/settings/{StaffTable,FeishuSheetConfigPanel}.tsx, src/lib/{types,version}.ts, src/routes/index.tsx, docs/{MIGRATIONS,PLAN,WORKLOG}.md
---
## 2026-09-18 10:44 UTC
[0918.111] 重新部署 Edge Functions: staff-sheets, attribution-sync-creators, feishu-read-connection-stats, feishu-read-bd-vids, feishu-read-editors, attribution-feishu（main 当前版本，无代码改动）。

