# 计划与任务板

> 规则：开工先认领（填工具名+日期+涉及文件），状态：⬜待办 / 🔶进行中 / ✅完成。
> 完成后把任务移到底部「已完成」区，并在 WORKLOG.md 追加一行。

## 当前计划

1. **自动授权**：每天北京 08:00 pg_cron → `/api/public/hooks/authorize-cron` → 循环执行授权 → 飞书机器人通知（已完成）
2. **GMV Max 性能优化**：短期索引已落库 ✅；中期 rollup 表、远期月分区待数据再上量后评估
3. **飞书基础数据自动同步**：每天北京 23:00 pg_cron → `/api/public/hooks/feishu-sync-cron` → 发样及素材统计 / 达人登记 / GMV 目标 / 站点交接 四个同步，早于 23:30 的归因快照（已完成）
4. **GMV 归因 V3 阶段 0**：①VN 站点林丽洪起始日期 **2024-01-01 已确认**（2026-09-16），`staff_site_permissions` 里那行备注的「待确认」字样只是文案，不影响判定；②`attribution_posted_at_gap` 已跑，**缺「发布时间」的 GMV 占比约 15%**，超过计划 §7.5 的 5% 阈值。但「缺发布时间」≠「会进 PENDING」——被 VID 强归因命中的行和商品卡片都不需要发布时间，需再按「缺发布时间 且 非商品卡片 且 VID 未登记」的口径算一遍真实影响面，再决定是否重新导出历史文件覆盖上传。**这个决定是阶段 3.1 的前置条件**：3.1 上线后这类行会进 PENDING（不计入任何人 KPI，但仍留在 GMV 总额里）
5. **前端「上次自动刷新时间」UI**：展示 `gmv_max_sync_state` 最近运行时间（未做）

## 任务板

| 任务 | 状态 | 认领 | 涉及文件 | 备注 |
| --- | --- | --- | --- | --- |
| 飞书表名称配置化（去掉 sheet 别名兜底）+ 设置页新子 tab | ✅ 完成 2026-09-18（**待人工：跑 migration `20260918170000_feishu_sheet_config.sql`；重新部署 `attribution-feishu`**） | claude 2026-09-18 | supabase/migrations/20260918170000_feishu_sheet_config.sql、supabase/functions/attribution-feishu/index.ts、src/components/settings/FeishuSheetConfigPanel.tsx、src/routes/settings.tsx、src/lib/attributionApi.ts | 新表 `feishu_sheet_config`（8 行初始数据 = 现有硬编码全量）：飞书表格名称 / sheet 名称 / 读取列范围 / 读写 / 说明 / 列映射(jsonb)。代码改成从配置读 sheet 名并**只做精确匹配**，删掉 SHEET_ALIASES 别名猜测；对不上直接报错并列出该表格现有 sheet。设置页新增「飞书表名称」子 tab，可改表名/范围/备注并即时生效（无需改代码与部署），每行一个「映射」按钮展开列级说明。 |
| 归因审查页重做（可判性）+ 归因查询页 | ✅ 完成 2026-09-18（**待人工：跑 migration `20260918140000_attribution_lookup.sql`；重新部署 `attribution-feishu`、`attribution-sync-creators`、`attribution-upload`**） | claude 2026-09-18 | src/components/attribution/{ReviewPanel,AttributionLookupPanel}.tsx、src/lib/attributionApi.ts、src/routes/gmv-attribution-admin.tsx、supabase/functions/{attribution-feishu,attribution-sync-creators,attribution-upload}/index.ts、supabase/migrations/20260918140000_attribution_lookup.sql | ①审查列表去掉 500 上限（改分页拉全量），前端每页 10 条 + 类型筛选；②每条冲突改成「冲突维度 + 候选归属/依据/站点/首次登记/最近登记」表格，KEYTYPE_CONFLICT 之前没有渲染分支、直接吐 JSON，现在补齐并在后端补上国家与两边日期；③判定改下拉，冲突双方与「忽略」排最前，其余同事在 optgroup 里，名单来自新 action `list-staff`（含离职）；④「归因结果」tab 新增归因查询：VID 精确 / 昵称模糊 → 每月归给谁 + 登记依据 + 当前归属，每页 10 行，走 RPC 返回单 JSON 避开 PostgREST 1000 行截断。 |
| 审查项 Excel 批量处理（导出填写 → 导入落库）+ 人工判定/人工覆盖分流 | ✅ 完成 2026-09-17（`attribution-feishu` 已于 2026-09-18 重新部署；migration 无新增） | claude 2026-09-17 | supabase/functions/attribution-feishu/index.ts、src/components/attribution/{ReviewExcelPanel,ReviewPanel}.tsx、src/lib/attributionApi.ts、src/routes/gmv-attribution-admin.tsx | 新 action `bulk-judgment`（不碰飞书、放在取 token 之前）。四种判定类型分别落到不同的表：JUDGE→attribution_review+creator_alias(MANUAL)；OVERRIDE_VID→attribution_manual_rules（最高优先级）；OVERRIDE_CREATOR→attribution_manual_decisions；IGNORE 只标已处理。导入先 dry_run 整批校验，有一行错就全部不写并按行号报错。**下拉受限**：SheetJS 社区版写不了 Excel 数据验证，模板用第二个 sheet 列可选值 + 导入强校验代替。**人工覆盖表要等阶段 3 引擎接入后才在归因中生效**，人工判定立即生效。 |
| 修「同步达人登记」150 秒超时（改分片续跑）+ 登记盘点视图 | ✅ 完成 2026-09-17（**待人工：跑 migration `20260917120000_registry_audit_views.sql`**） | claude 2026-09-17 | supabase/functions/attribution-sync-creators/index.ts、supabase/migrations/20260917120000_registry_audit_views.sql、src/lib/attributionApi.ts、src/routes/gmv-attribution-admin.tsx、src/routes/api/public/hooks/feishu-sync-cron.ts | 日志确认：函数每次都是 booted → 150 秒后 shutdown，没有任何 error —— 是被平台墙钟上限掐断，所以前端只看到「非 2XX」。登记数据已 6 万行，一次做不完。改成按 sheet 分片、时间预算 110 秒，每片先删后插自己那个 source_sheet（中断不留半张表），返回 done/next，前端与 cron 循环续跑；归属解析改成从数据库读全表（分片后内存里只有最后一片）。 |
| 保护期统一 90 自然天（归因 + 报表同口径）+ 发布时间口径修正 + 一致性校验 SQL | ✅ 完成 2026-09-16（**待人工：跑 migration `20260916160000_protection_90d_consistency.sql`；部署后点一次「同步达人登记」让归属按 90 天重算（5 个 Edge Function 已于 2026-09-17 重新部署）**） | claude 2026-09-16 | supabase/functions/_shared/{attribution,stages,attribution.test}.ts、supabase/migrations/20260916160000_protection_90d_consistency.sql、src/routes/gmv-attribution-admin.tsx | **会改归因数字**：正好卡在 89/90 天与月份长度差异上的历史转移会被重判（计划 §八 3.3 提前做）。全项目已无「3 个月」保护期的代码或文案；SQL 侧本来就没实现过保护期。新增独立 SQL 第二实现 `attribution_protection_owner_90d` + 校验视图 `attribution_protection_check_90d`，已用 5 个边界用例在本地 PostgreSQL 16 与 TS 引擎逐行比对一致。 |
| GMV 归因 V3 阶段 1+2：测试基建 + 事实层/身份层/区间层建模（只生成不使用） | ✅ 完成 2026-09-16（**待人工：跑 migration `20260916140000_attribution_v3_fact_tables.sql`、`20260916140100_attribution_v3_identity_stages.sql`**；Edge Function `attribution-identity-build` 已部署、`attribution-feishu` 已重新部署） | claude 2026-09-16 | supabase/functions/_shared/{identity,stages,attribution.test,identity.test}.ts、supabase/functions/attribution-identity-build/index.ts、supabase/functions/attribution-feishu/index.ts、supabase/migrations/2026091614*.sql、src/components/attribution/IdentityPreviewPanel.tsx、src/lib/attributionApi.ts、src/routes/gmv-attribution-admin.tsx | 按 GMV_ATTRIBUTION_V3_PLAN v3.0。**归因数字不变**：新表只生成不使用，引擎仍读 creator_ownership。阶段 1 = 62 个 Deno.test 纯函数用例（固化现状 + 身份/区间新逻辑）；阶段 2 = 9 张表 + 2 个对账视图 + 生成函数 + 预演面板。两份 migration 已在本地 PostgreSQL 16 实跑验证（区间重叠/权限区间重叠/规则形状约束都按预期被拒绝，权限表 14 行、交接表 2 行导入正确）。 |
| GMV 归因 V3 阶段 3：引擎切换（会改数字，逐项单独提交） | ⬜ 待办 | — | supabase/functions/_shared/attribution.ts 等 | 顺序见计划 §八：①posted_at 必填（**指表头必须有这一列，不是每行都有值**：直播/商品卡本来就没有发布时间）+ posted_site_date ②发样/回收双事件 ③90 自然天（**已于 2026-09-16 提前完成，见下方已完成区**）④交接并入事件流 ⑤站点权限校验 ⑥Pass 2 改 creator_key + 按发布日期命中区间 ⑦PENDING/EXCLUDED 两桶 + 都不算规则 ⑧人工判定事实表接入 ⑨pickVidOwner 去重键改 staff+role。每项都要出全站点前后差异表。 |
| GMV 归因 V3 阶段 4：事实层维护页（都不算规则 / 站点权限 / 权限启用日期）+ 冲突表阻塞·提示分离 | ⬜ 待办 | — | src/routes/、src/components/attribution/ | 阶段 2 已建表并导入初始数据，维护 UI 留到这里做：需要 migration 先在 Supabase 跑完、`src/integrations/supabase/types.ts` 重新生成后才好接。 |
| 飞书基础数据每晚自动同步（发样及素材统计 + 达人登记 / GMV 目标 / 站点交接） | ✅ 完成 2026-09-16（**待人工：跑 migration `20260916120000_feishu_sync_cron.sql`；重新部署 `feishu-read-connection-stats`、`attribution-sync-creators`、`attribution-feishu`**） | claude 2026-09-16 | src/routes/api/public/hooks/feishu-sync-cron.ts、supabase/migrations/20260916120000_feishu_sync_cron.sql、supabase/functions/_shared/cron.ts、supabase/functions/{feishu-read-connection-stats,attribution-sync-creators,attribution-feishu}/index.ts、src/routes/{connection-stats,gmv-attribution-admin}.tsx | pg_cron job `feishu-sync-nightly`（`0 15 * * *` = 北京 23:00）→ 新服务端路由串行跑四个同步，排在归因快照（23:30）之前半小时，保证当晚快照用的是当天的登记/目标/交接数据。单步 4 分钟超时、失败不影响后面几步，结果写 `gmv_max_sync_state('feishu_nightly_sync')`，有失败发飞书机器人通知。cron 免口令统一走 `_shared/cron.ts`，`attribution-feishu` 只放行两个纯同步 action。 |
| GMV 归因进度（按审核计划重构） | ⏸ 已由下方「GMV 归因 V1 落地」接手/取代 2026-09-09 | codex 2026-07-11 → claude 2026-09-09 | docs/GMV_ATTRIBUTION_REVIEW_PLAN.md、supabase/migrations/、supabase/functions/attribution-*、supabase/functions/_shared/{attribution,attribution-report}.ts、src/routes/gmv-attribution*.tsx、src/components/attribution/*、src/lib/{attributionApi,adExcel,tabs}.ts、src/routes/settings.tsx | 该任务近两个月未推进（认领于 2026-07-11），批次固化/定时刷新/原子切换/目标组/2000 美元阈值展示规则等第 7-11 节内容已过时。2026-09-09 claude 按新方案 `GMV_ATTRIBUTION_V1_PLAN.md` 接手同一批文件（范围更小、已落地，见下一行任务），本行仅保留历史记录，不再单独推进；`attribution_batches`/`attribution_batch_details`/`attribution_run_state` 三张表继续保留不接线。 |
| GMV 归因 V1（Excel 上传合并汇总 + 汇率修正 + 唯一VID导出 + 审查判定 + 上传体验） | ✅ 完成 2026-09-09（`attribution-upload`、`attribution-feishu` 已重新部署） | claude 2026-09-09 | supabase/functions/{attribution-upload,attribution-feishu}/index.ts、supabase/functions/_shared/attribution-report.ts、src/routes/gmv-attribution*.tsx、src/components/attribution/{UploadView,ReviewPanel,UploadStatusMatrix,UnmatchedTrendTable}.tsx、src/components/settings/ExchangeRateCard.tsx、src/routes/settings.tsx、src/lib/{api,attributionApi}.ts | 见下方「已完成」条目详细说明（第二轮：修复上传去重误伤分卷文件、大文件 finalize 报错、飞书判定读回列错位；新增网页端直接判定+双写飞书、上传历史分页与状态矩阵、无建联达人12个月趋势表、GMV归因页表格化）。 |
| GMV 归因 V2（大文件上传进度/断点 + 归因口径自查） | ✅ 完成 2026-09-10（**待人工：重新部署 attribution-upload**） | claude 2026-09-10 | src/lib/uploadQueue.ts、src/components/attribution/{UploadView,DiagnosePanel}.tsx、src/routes/gmv-attribution-admin.tsx、src/lib/attributionApi.ts、supabase/functions/attribution-upload/index.ts | ①上传队列与上传循环挪到模块级 store，切标签页/切页面回来进度不丢；状态列右侧新增「进度」列（百分比+已传行数/总行数+当前阶段），sessionStorage 镜像使刷新后仍能看到上一轮进度 ②分批 1000→2000、3 并发、单批失败指数退避重试 3 次 ③finalize 只读归因需要的 9 列、回填只发主键+4 个 attr 列（原来 22 列全量回传是 10 万行卡在「上传中」的主因）④上传历史对非 READY 批次新增「重新归因」按钮，行数已传完不必重传 ⑤新增 `diagnose` action + 「归因口径自查」面板：逐批次统计商品卡/带VID行/VID命中/昵称同站点命中/昵称登记在别站点/从未登记，并对照上传站点与建联表站点写法，直接指出归因断在哪一层 |
| GMV 归因 V3（上传与归因解耦 + 飞书 ID 按文本读 + 站点英文简写） | ✅ 完成 2026-09-10（**待人工：跑 migration 20260910120000_ad_upload_agg.sql；重新部署全部飞书相关 Function**） | claude 2026-09-10 | supabase/migrations/20260910120000_ad_upload_agg.sql、supabase/functions/attribution-upload/index.ts、supabase/functions/attribution-sync-creators/index.ts、supabase/functions/_shared/{feishu,cells,attribution}.ts、supabase/functions/feishu-read/index.ts、src/routes/gmv-attribution-admin.tsx、src/components/attribution/{UploadView,DetailTable,DiagnosePanel}.tsx、src/lib/{attributionApi,uploadQueue,version}.ts | ①新增 `ad_upload_agg` + RPC `attribution_build_upload_agg`：finalize 只在数据库内按 (VID,达人昵称,商品ID,内容类型,币种) 归并，不再计算/固化归因；所有报表口径（get/merged/detail/export_vid_summary/unmatched_trend/site_mismatch/diagnose）改为读归并表 + 现算归因，`attr_*` 四列废弃 ②`readRange` 默认 `valueRenderOption=ToString`，所有单元格按文本取值，杜绝 19 位 VID / 商品ID 丢精度；`parseDate` 增加「年在前」精确匹配以适配文本日期 ③站点写法统一英文简写：`create` 拒收汉字站点、`attribution-sync-creators` 返回 `cjk_sites` 计数并在前端红字提示、诊断面板增加汉字站点结论 ④「数据准备」三个按钮说明改为按钮右侧「?」弹出 |
| GMV 归因 V4（两层存储 + 每晚快照 + 页面状态不丢） | ✅ 完成 2026-09-10（**待人工：跑 2 个 migration；重新部署 attribution-upload**） | claude 2026-09-10 | supabase/migrations/{20260910140000_attribution_runs,20260910150000_attribution_cron}.sql、supabase/functions/attribution-upload/index.ts、src/routes/api/public/hooks/attribution-cron.ts、src/routes/{gmv-attribution,gmv-attribution-admin}.tsx、src/lib/{attributionView,attributionApi,uploadQueue}.ts | ①第二层存储 `attribution_runs`+`attribution_run_rows`：一次「全站点全人员」归因的结果快照与明细，带历史（每月保留最近 10 条）②新增 `refresh`/`report`/`report_detail`/`runs` 四个 action，前台默认读最新快照秒开，下钻直接查快照明细表不重算 ③每晚北京 23:30 pg_cron → `/api/public/hooks/attribution-cron` → `refresh` 最近 3 个月；上传完成后自动刷新该月快照；页面上「重新计算」随时手动刷 ④两个归因页面的月份/报表/明细状态移到模块级 store + sessionStorage，切标签页、切路由、刷新页面都不再丢 |
| GMV 归因 V5（历史批次自愈 + cron 条件刷新 + 站点写法收口 + 上传列表可干预） | ✅ 完成 2026-09-11（**待人工：跑 migration 20260911090000_agg_group_by_site.sql 后删除该文件；重新部署 attribution-upload、attribution-sync-creators**） | claude 2026-09-11 | supabase/migrations/20260911090000_agg_group_by_site.sql、supabase/functions/attribution-upload/index.ts、supabase/functions/attribution-sync-creators/index.ts、supabase/functions/_shared/attribution.ts、src/lib/{uploadQueue,attributionApi,version}.ts、src/components/attribution/UploadView.tsx | ①**修「重新计算后报表全 0」**：解耦改造之前 finalize 的历史批次没有 ad_upload_agg 行，生成快照/自查时自动补跑归并（ensureAgg），不用逐个点重跑 ②cron 刷新加 `skip_if_unchanged`：该月广告表和达人登记都没变化就跳过，不再每晚白算；手动「重新计算」仍强制重跑 ③归并键显式带站点；归并层只存可相加数值，比率一律重算 ④站点写法：新增 `normalizeSiteCode` 等效映射（PH本土→PHL）与 `isHeaderLikeSite`（「地区/店铺」等表头、空值不再误报为汉字站点）⑤上传列表刷新后不再残留传不了也删不掉的「待上传」行，每行新增移除按钮 |
| GMV 归因 V6（快照聚合下沉数据库 + 归因去重计数 + 数据准备进度面板） | ✅ 完成 2026-09-11（**待人工：跑 migration 20260911120000_attribution_snapshot_sql.sql 后删除；重新部署 attribution-upload、attribution-sync-creators**） | claude 2026-09-11 | supabase/migrations/20260911120000_attribution_snapshot_sql.sql、supabase/functions/attribution-upload/index.ts、supabase/functions/attribution-sync-creators/index.ts、supabase/functions/_shared/attribution-report.ts、src/components/attribution/{DataPrepPanel,DiagnosePanel,StaffCountryTable,ProgressBoard,UploadView}.tsx、src/lib/attributionApi.ts | ①修「重新计算 non-2xx」：10 万行批次归并后仍有 9.4 万行，整月几十万行拉进 Edge Function 必超时。改为「判定在 Edge Function、聚合在数据库」——只拉去重后的判定键（站点×VID×昵称×内容类型），判定结果写 attribution_run_keys，再由 attribution_apply_run 在库内 JOIN 归并表+汇率写明细并返回几百行紧凑汇总 ②归因口径新增去重计数：站点×同事的归因 VID 数 / 归因达人昵称数，与 GMV 并列展示 ③新增「数据准备进度」面板：达人登记一人一行×固定站点列（VID/达人）、当月 GMV 目标、站点交接列表 ④建联表按表头自动识别粉丝量列写入 creator_registry/creator_ownership.follower_count（前台暂不展示）⑤上传按钮改名「上传解析数据」、上传历史默认显示状态矩阵 ⑥site_mismatch/unmatched_trend/get(merged) 全部改读快照，diagnose 改按判定键统计并按站点分组 |
| GMV 归因 V7（归并即折美元 + 去重计数只按同事×国家） | ✅ 完成 2026-09-11（**待人工：跑 migration 20260911170000_agg_usd_and_distinct.sql 后删除；重新部署 attribution-upload**） | claude 2026-09-11 | supabase/migrations/20260911170000_agg_usd_and_distinct.sql、supabase/functions/attribution-upload/index.ts、supabase/functions/_shared/attribution-report.ts、src/components/attribution/{StaffCountryTable,ProgressBoard}.tsx | ①`ad_upload_agg` 新增 usd_rate/gmv_usd/cost_usd，归并时就按后台汇率折美元并记录当时汇率；后续环节只做加法，不再有汇率分支；新增 RPC `attribution_rebuild_agg_usd(month)` 在汇率变更后就地重算（生成快照时自动调用）②去重计数改为数据库 `attribution_run_distinct` 用 GROUPING SETS 一次算出 CELL(同事×国家)/STAFF(同事)/TOTAL 三个粒度的精确 count(DISTINCT)，口径固定为「只按同事+国家」，对整月合并后的集合去重；删掉原先在 JS 里 max/sum 的估算 ③顶部「归因 VID 数」只统计真正归到人的部分（原来把商品卡和无建联也算了进去） |
| GMV 归因 V8（内容类型四分类 + 修 migration 的 UPDATE…LATERAL 报错） | ✅ 完成 2026-09-11（**待人工：让 Lovable 按序执行 20260911170000/170100/170200 三个 migration 并重新部署 attribution-upload**） | claude 2026-09-11 | supabase/migrations/20260911170000_agg_usd_and_distinct.sql、supabase/functions/_shared/{attribution,attribution-report}.ts、supabase/functions/attribution-upload/index.ts、src/components/attribution/{TypeMixPanel,ProgressBoard,DiagnosePanel,DetailTable}.tsx、src/lib/attributionApi.ts | ①修 `ERROR 42P10: invalid reference to FROM-clause entry for table "a"`——UPDATE 的目标表不能被 FROM 里的 LATERAL 反向引用，三处改成「先做币种→汇率小表再 JOIN」②内容类型四分类：视频=VID+昵称（BD/剪辑）、直播=只按达人昵称归 BD（不走 VID）、商品卡=不归人但金额入库、其他=新增 OTHER 桶（`normalizeCreativeType` 不再把认不出的类型默认当视频）③新增 RPC `attribution_run_by_type` + 前端「内容类型占比」面板：各站点 GMV 按四类的金额与占比 |
| GMV 归因 V9（站点交接改为「新 BD 动作触发」） | ✅ 完成 2026-09-13（**纯判定逻辑改动，无 migration；待人工：重新部署 attribution-upload**） | claude 2026-09-13 | supabase/functions/_shared/{attribution,attribution-report}.ts、docs/GMV_ATTRIBUTION_REVIEW_PLAN.md、src/components/attribution/DataPrepPanel.tsx、src/routes/gmv-attribution-admin.tsx | 交接表上的日期不再等于转移生效日，只作为「允许转移」的起点。实际转移日 = 新 BD 在交接日（含）之后，对**那个具体达人**第一次登记/发样的日期：新 BD 从没接手过该达人 → 这条交接对他永不生效，仍归原 BD，不再因为交接日到了就把历史达人批量转给新人；发布时间 < 转移日的素材仍算原 BD，≥ 转移日才切新 BD；发布时间未知按当前归属（已转移）处理。只作用于昵称/账号路径，VID 强匹配不受影响。`loadAttrContext` 新增 `creatorActions`（站点×达人 → 同事 → 登记动作日期升序）供判定使用；VID 推断发布时间的 ±5 天审查提示改为围绕**实际转移日**而不是交接表日期。本轮是本次计划里唯一会改变归因结果的改动，故单独一个 commit。 |
| GMV 归因 V9b（归属五分类枚举函数） | ✅ 完成 2026-09-13（**纯新增函数，暂无调用方；无 migration，无需部署**） | claude 2026-09-13 | supabase/functions/_shared/attribution.ts | 新增 `OwnershipClass`（商品卡片 / 剪辑 / BD-VID / BD-账号 / 未建联达人 / 其他）与 `classifyAttribution(bucket, role, matchType)`，把系统内部的 (桶, 角色, 匹配方式) 折成月度 Excel 流程「归属角色」列的那几档，两边对数据不用再互相翻译。`其他` 是本系统后加的桶（创意类型认不出来的行），Excel 流程里没有对应项。本次只落函数，不改任何现有判定与展示。 |
| GMV 归因 V10（**找到「大部分归不上」的真因**：判定键只读了前 1000 个） | ✅ 完成 2026-09-14（**待人工：重新部署 attribution-upload**） | claude 2026-09-14 | supabase/functions/attribution-upload/index.ts、supabase/functions/_shared/attribution-report.ts、docs/GMV_ATTRIBUTION_DIAGNOSE_SQL.md | PostgREST 的服务端单次返回行数上限（Supabase 默认 1000）对 RPC 同样生效：`attribution_month_keys` 传 `_limit: 5000` 实际只回 1000 行，而翻页的结束条件写的是「本页行数 < 请求页大小」，于是第一页就判定读完——整月 167,895 个判定键只判了前 1000 个，其余在 `attribution_apply_run` 的 `LEFT JOIN … coalesce(k.bucket,'UNMATCHED')` 里被静默兜成无建联。判定键按 `country` 排序，JP 排第一，所以表现成「只有 JP 归得上，其它站点 100% 无建联」。9/11 快照的数字完全对得上：JP 共 1,369 个键，前 1,000 个被判定（649 归到人 + 351 无建联），剩余 369 个未判定兜成无建联，合计 720 —— 与 G1 的 JP UNMATCHED 720 一致。修法：抽出 `fetchMonthKeys`，页大小对齐服务端上限、按实际返回行数推进 offset、空页才结束，并与 `attribution_month_key_count` 对账，数量对不上直接报错；诊断面板同一处翻页一并改掉（此前诊断结论也是只看了前 1000 个键）。另加 `assertNotCapped` 守住 apply_run/distinct/by_type 三个汇总 RPC，顶到上限即报错而不是少算钱。顺带修 `pageAll`/`pageAllRows` 11 处 LIMIT/OFFSET 翻页缺 ORDER BY 的隐患。 |
| GMV 归因 V11（大批次归并分片 + 失败批量重试） | ✅ 完成 2026-09-14（**待人工：跑 migration 20260914120000_finalize_chunked.sql；重新部署 attribution-upload**） | claude 2026-09-14 | supabase/migrations/20260914120000_finalize_chunked.sql、supabase/functions/attribution-upload/index.ts、src/lib/{attributionApi,uploadQueue,version}.ts、src/components/attribution/UploadView.tsx | ①修「100,000/100,000 行 → 归并失败：upstream request timeout」：10 万行的归并在一次 HTTP 请求里做不完，而整个归并是一个事务，网关超时即回滚，重试多少次都是同样结果。拆成 `attribution_clear_upload_agg` → `attribution_build_upload_agg_part`（按归并键哈希分片，同组永远落同一片，`ON CONFLICT DO NOTHING` 保证重跑不重复）→ `attribution_finalize_mark`（汇率校验+回填合计+置 READY），每片独立事务、独立请求，超时也只丢一片。后端超过 2 万行自动转分片并返回 `{staged, parts}`，`uploadApi.finalize` 内部逐片驱动，调用方签名不变；上传队列和重新归并按钮都会显示「第 N / M 片」。小文件仍走原来的一次性 `attribution_finalize_upload`。②上传历史新增「失败重试（N）」按钮：对所有未完成批次串行重跑归并，行数据还在库里，不用重传文件；单个失败不影响后续，结束后汇总成功/失败数。 |
| GMV 归因 V12（快照重算把往返次数从 250+ 降到 20 + 死快照收尸） | ✅ 完成 2026-09-14（migration 已应用、attribution-upload 已重新部署） | claude/lovable 2026-09-14 | supabase/migrations/20260914160000_snapshot_bulk_io.sql、supabase/functions/attribution-upload/index.ts | 「重新计算失败：快照重算超时」的瓶颈是**请求次数**不是 SQL：PostgREST 单次最多回 1000 行，16 万+ 判定键要来回 168 次 HTTP 读、再按 2000 行一批写回 84 次，250 多次串行往返超过 Edge Function 后台任务的墙钟上限，进程被掐断，`attribution_runs` 那行永远停在 RUNNING，前端轮询等满 20 分钟才报超时。①新增 `attribution_month_keys_json`（整页打成一个 JSON 即一行返回，绕开 1000 行上限，一次两万）与 `attribution_write_run_keys(jsonb)`（一次写两万），往返 250+ → 20 上下 ②新增 `attribution_runs_fail_stale(minutes)`，把 RUNNING 超过 10 分钟的死快照标成 FAILED 并写明原因；`refresh` 开头与 `runs` 轮询各调一次，前端几分钟内就能看到「后台重算进程已中断」而不是干等 20 分钟。 |
| GMV 归因 V13（**CPU 配额**：候选键筛选下沉数据库） | ✅ 完成 2026-09-14（**待人工：跑 migration 20260914180000_snapshot_candidate_keys.sql；重新部署 attribution-upload**） | claude 2026-09-14 | supabase/migrations/20260914180000_snapshot_candidate_keys.sql、supabase/functions/attribution-upload/index.ts | 日志坐实真因是 `CPU Time exceeded` 而非超时：2026-08 有 250,296 个判定键，读取已优化到 47 秒（13 次往返），但把 25 万个键解析成 JS 对象、跑引擎、再序列化写回，超出 Edge Function 单次调用的 CPU 预算。关键观察：这些键里绝大多数 VID 对不上、昵称也查无此人，引擎结论必然是 UNMATCHED，商品卡/认不出的类型更是只看内容类型就能定桶——完全不必进 JS。新增 `attribution_month_keys_ext`（整月键 + 是否候选）、`attribution_seed_run_keys`（一次扫描分两路：非候选按内容类型直接定桶写 `attribution_run_keys`，候选写落地表 `attribution_run_candidates`）、`attribution_candidate_keys_json(run_id,…)`（只读落地表翻页）。判定逻辑仍只有 TS 引擎一份，SQL 只回答「这条有没有可能归到人」。候选判据刻意偏宽且与引擎逐条对齐：VID 命中 creator_registry/staff_vid_map、昵称命中 creator_ownership/creator_alias、或落在别名投票名单（VID 命中的视频行的同名达人）内；昵称/站点/内容类型三个归一化函数与 TS 的 normalizeName/identityKey/normalizeCreativeType 一一对应（用 PG 的 `normalize(…, NFKC)` 对齐 NFKC）。 |
| GMV 归因 V14（判定按站点分片、判完一片存库再继续） | ✅ 完成 2026-09-14（**待人工：按序跑 20260914180000 与 20260914200000 两个 migration；重新部署 attribution-upload**） | claude 2026-09-14 | supabase/migrations/20260914200000_snapshot_chunked_judge.sql、supabase/functions/attribution-upload/index.ts、supabase/functions/_shared/attribution-report.ts、src/lib/attributionApi.ts、src/routes/api/public/hooks/attribution-cron.ts | 用户 Excel 透视对账证明候选筛选不够：2026-08 共 250,275 条素材，**38.9% 真能归到人**（MX-AR 65%、MX-NE 78%、US 71%），候选只能压到约 12 万，仍超 Edge Function 的 CPU 配额。改为按用户提出的方式分片：一次请求只判一片（15000 个键），判完写库并从候选表删掉，剩余行数即进度，然后继续下一片。分片按 (站点, 归一化昵称) 切——别名投票只在同一 (站点,昵称) 内产生和消费，同组不拆开则分片与一次性判定结果等价。上下文同步按站点收窄（登记/归属/别名/交接/登记动作），再用 `attribution_vid_regs_for_country` 补回「VID 登记在别站点」的那部分，保持 VID 强匹配的跨站点语义不变。`refresh` 拆成 建分片 / judge / finish 三个 stage，由前端与 cron 路由分别驱动（两边都不在 Edge 里循环，否则 CPU 照样累加）。|
| GMV 归因 V15（导出带归属 + 两个按钮加「?」+ 无建联表分页/排序 + 修趋势表截断） | ✅ 完成 2026-09-15（**待人工：跑 migration 20260915090000_export_owner_and_unmatched.sql；重新部署 attribution-upload**） | claude 2026-09-15 | supabase/migrations/20260915090000_export_owner_and_unmatched.sql、supabase/functions/attribution-upload/index.ts、src/routes/gmv-attribution-admin.tsx、src/components/attribution/UnmatchedTrendTable.tsx、src/lib/attributionApi.ts | ①「导出唯一VID汇总」「回写飞书进度」两个按钮右侧加「?」说明，写明导出列顺序、回写到哪个 sheet 与 A–S 列含义 ②导出新增归属四列（归属人/角色/归属类别/匹配方式），由新 RPC `attribution_run_owner_by_vid` 从该月最新快照按 (站点,VID) 取金额最大的判定——这是与本地 Excel 逐行对账的底表 ③无建联达人表 20 行/页，默认按所选月份 GMV 降序（原来按 12 个月合计，会把早就不投的老达人顶到前面）④**修趋势表截断**：原来每月只取前 5000 条无建联明细再聚合，而一个月有几万条，长尾被整段砍掉、月度数字偏小且跨月不可比；改为新 RPC `attribution_run_unmatched_by_creator` 在库内按 (站点,归一化昵称) 全量聚合。 |
| GMV Max 原始导出 + 多国广告表归因验证 | 🔶 进行中 | codex 2026-07-13 | supabase/functions/gmv-max-raw-export/index.ts、src/routes/api-test.tsx、docs/{ARCHITECTURE,PLAN,WORKLOG}.md、src/components/attribution/UploadView.tsx（仅验证） | 新增独立、只读的 GMV Max CSV 导出 Function，按广告户与日期范围拉取；不改变 gmv-max-sync。优先验证同月多国家多 Excel 上传、归因及合并展示。 |
| 前端显示「上次自动刷新时间」 | ⬜ | — | src/routes/feishu-data.tsx | 读 gmv_max_sync_state（cron_yesterday / cron_today） |
| GMV Max 广告组新建 API | ✅ 完成（已部署 2026-08-19 lovable） | claude 2026-08-19 | supabase/functions/gmv-max-adgroup-create/index.ts、supabase/functions/_shared/tiktok.ts、src/routes/api-test.tsx | TikTok GMV Max 无独立 campaign/adgroup/ad 三层，新建广告组即调用 `/campaign/gmv_max/create/`（含预算/商品/出价，等价于把广告组打包进 campaign）。新函数只做鉴权+按 advertiser_id 查 token+透传 POST，不校验/拼装业务字段，需在 API 测试页命令框手填完整 TikTok GmvMaxCreateBody JSON（含 request_id 保证幂等）。真实写调用，会产生真实花费，测试前确认参数。新增 `_shared/tiktok.ts` 的 `ttPost`（同 `ttGet` 限速/退避）。 |
| GMV MAX新建页面（表单+Excel批量） | ✅ 完成（已部署 2026-08-25 lovable） | claude 2026-08-25 | src/routes/gmv-max-create.tsx、src/lib/tabs.ts、supabase/functions/gmv-max-adgroup-batch-create/index.ts、supabase/functions/_shared/gmv-max-adgroup.ts、supabase/functions/gmv-max-adgroup-create/index.ts（重构复用共享逻辑）、supabase/functions/{tiktok-oauth-exchange,tiktok-connection-save,tiktok-connections}/index.ts、src/routes/oauth.tiktok.callback.tsx、src/lib/store.ts、src/components/settings/AccountsTable.tsx、supabase/migrations/20260825120000_tiktok_connections_bc_name.sql | 新 tab「GMV MAX新建」（放在素材成效下、GMV归因上）。页面：①单个新建表单——广告户下拉（自动读 store_id/BC ID，缺失会提示去设置页补）、广告组名称、商品ID多行文本框、ROI、预算、开始/结束时间（datetime-local，默认马上开始/不设结束）；②Excel 批量新建——同一套字段的表格上传（含"下载模板"按钮），前端用 xlsx 解析、校验后调同一个批量接口。新 Edge Function `gmv-max-adgroup-batch-create` 接受结构化 `{rows:[...]}`，自动从 `advertiser_countries.shop_id`/`tiktok_connections.bc_id` 取 store_id/store_authorized_bc_id，固定其余字段（PRODUCT/CUSTOMIZED_PRODUCTS/VALUE/VO_MIN_ROAS/AUTO_SELECTION），逐行创建、单行失败不影响其余行。抽出 `_shared/gmv-max-adgroup.ts`（`findConnectionsForAdvertiser`/`createGmvMaxCampaign`/`genRequestId`）给这个新函数和原 `gmv-max-adgroup-create` 共用。顺带在设置页「TikTok 授权连接」表加 BC 名称/BC ID 两列（`tiktok_connections` 新增 `bc_name` 列，`tiktok-oauth-exchange` 授权时顺带查 `/bc/get/` 拿名称）。① 迁移、② 五个 Edge Function 部署已于 2026-08-25 由 lovable 完成。**待人工**：③ 有权限的账号需要在账号管理里给自己加 `gmv-max-create` tab 权限才能看到新 tab（管理员账号不受限）④ 真实写调用，首次用小额预算/短时间验证后再放量。 |
| 发样及素材统计 tab | ✅ 完成（**已重新部署 connection-stats-query 2026-09-09**；权限待人工授权） | claude 2026-09-09 | supabase/migrations/20260826130000_connection_material_registry.sql, supabase/functions/{gmv-max-identity-get,feishu-read-connection-stats,connection-stats-query}/index.ts, src/routes/connection-stats.tsx, src/components/AppShell.tsx, src/lib/tabs.ts, src/components/settings/DataSyncCard.tsx, src/lib/version.ts | 新 tab「发样及素材统计」（放在素材成效上方），只读展示，不回写飞书。数据源：5 张 BD「建联-xxx」表（A2:Q）+ 剪辑登记表 2 个 sheet（B2:G），经 `feishu-read-connection-stats` 同步进新缓存表 `connection_material_registry`（按 source_sheet 先删后插），`connection-stats-query` 做聚合返回给页面。口径：发样记录=(达人,国家,SKU)三元组去重；BD 回收有效日期=N列视频登记日期（非O列发布日期）；剪辑回收有效日期=C列日期(发布日期)；只看 VID 格式不校验授权码；国家占比/粉丝分层两个饼图只统计 BD；粉丝分层按各达人自己所在国家门槛定档后跨国家合并；BD 日均回收/剪辑日均产出都按等效工作日（一~五各1天、周六0.5天、周日0天）折算；country 字段同步时统一转大写；`connection-stats-query` 加 `include_meta` 避免全表扫下拉项；回收明细分组维度为 `group_country`/`group_sku` 两个独立布尔。**2026-09-08 修复 SKU 搜索**：之前的匹配函数要求「查询串本身是纯数字」，输入 AR333 这种带字母前缀完全不命中；改成按 token 分类——含数字的 token（"333"/"AR333"，忽略字母）按数字整段精确匹配（命中 333-A/AR333，不命中 3331/AR3331），纯字母 token（"B"/"K"）按大小写不敏感子串匹配整个 sku；前端多 token 分隔符加「&」（如 333&B = 333 数字整段 OR 含 B），输入框右侧加提示「用&可合并搜索，如 333&B」。**进展（2026-09-05 lovable 核查）**：①迁移已执行（36678 行）②函数已重新部署 ③country 已全部大写（0 行异常），无需再手动同步。**2026-09-09 [claude] 每日发样/回收图表加剪辑素材**：标题「发样数量 / 回收素材数量（BD）」去掉「（BD）」；`connection-stats-query` 的 `daily_series` 每项新增 `editor_recover`（剪辑当日回收计数，口径同已有的剪辑回收：vid 去重、有效日期=post_date）；前端第二根柱子改为 BD素材（蓝，底）+ 剪辑素材（红，堆叠在上）；发样/BD素材/剪辑素材三个图例改成可点击开关，各自独立控制对应柱子是否显示，Y 轴按当前勾选的数据重新缩放；回收率折线本质是 BD 口径（recover/sample），当发样和 BD素材两个开关都关闭时（含只剩剪辑素材一项）自动隐藏折线和右侧百分比轴。**待人工**：管理员在账号管理里给需要的账号加 `connection-stats` 权限。 |

## 进行中文件锁（防交叉修改）

（当前无进行中锁；2026-09-09 claude 完成 GMV 归因 V1 后解除上条 codex 2026-07-11 的锁，该任务已标记 ⏸ 取代，详见任务板。）

## 已完成

- ✅ 2026-09-11 [lovable] 复核两条项目监控告警：大文件上传超时引用的旧全量读取、逐行回填和即时全月重算已被数据库归并、快照和紧凑汇总取代，近期线上日志无同类超时，标记为过时；确认「清空全部批次」RPC 漏列 `ad_upload_agg` 会触发外键错误，新迁移将归并表、原始行和批次表一并 TRUNCATE，保留 service_role-only 权限。

- ✅ 2026-09-10 [lovable] 修复 10 万行上传 finalize 超时：新增 service-role-only 的 `attribution_finalize_upload` RPC，在数据库内原子完成归并、汇率校验、金额汇总和置 READY，并用 advisory lock 阻止同批次并发重算；Edge Function 不再读回 9 万余条归并结果生成即时预览，上传完成后统一刷新月度快照。

- ✅ 2026-09-10 [lovable] 修复 `attribution-upload finalize` 的 10 万行数据库超时：读取从 OFFSET 分页改为 `row_no` 游标分页；归因结果回填从通用 UPSERT 改为 service-role-only 的 `attribution_upload_rows_update(uuid,jsonb)` 专用 UPDATE RPC（120 秒独立预算），并增加读取/上下文/归因/产物/回填/汇总分阶段耗时日志。

- ✅ 2026-09-09 [lovable] 二次修复 `attribution-upload` 的 `unmatched_trend` 超时：线上日志确认 12 个月一次性 RPC 在并发/大月数据下仍撞约 20 秒语句预算；改为 12 次逐月数据库聚合后在函数内合并，完整保留全部月份与达人数据，同时将每条 SQL 限定为单月。已重新部署 `attribution-upload`。
- ✅ 2026-09-09 [lovable] 修复 `attribution-upload` 的 `unmatched_trend` 超时：确认原实现会把近 12 个月约 58 万条 UNMATCHED 明细逐页拉进 Edge Function；新增仅 service_role 可调用的 `attribution_unmatched_trend_json(text[])` RPC，在数据库内按国家×达人×月份折美元汇总，并以单个 JSON 返回完整结果，避免接口默认 1000 行截断；同时增加 UNMATCHED 专用覆盖索引。迁移已执行，`attribution-upload` 已重新部署。

- ✅ 2026-09-09 [claude] **GMV 归因第五轮（站点精确匹配 + 货币阻断 + 上传历史批量操作）**：①**站点改回按字母精确匹配、彻底去掉跨站点兜底**——删除 `addGlobalFallback`，`lookupIdentity` 只做 `identityKey(站点, 归一化名)` 精确查表；名字对得上但站点对不上的行一律归 UNMATCHED，不再静默改判。②**新增「站点未匹配确认」表**——`attribution-upload` 新增 `site_mismatch { month }` action：把 `creator_ownership` + `creator_alias` 按「归一化名字」建一份**只用于诊断、不参与归因**的索引，扫该月 UNMATCHED 行，凡是名字在建联/别名表里登记过（只是登记在别的站点）的，按 (上传站点, 达人昵称) 聚合出行数 / GMV(USD) / 登记站点 / 登记归属 BD / 登记来源。前端新增 `SiteMismatchTable.tsx`，挂在「月度进度」报表下方，可查询 + 导出 xlsx。**对已归因的历史批次直接生效，不用重新上传**。③**「货币」列改为必填、缺失直接阻断上传**——`currency` 加进 `REQUIRED`，缺列时 `parseAdExcel` 直接 throw（文件进不了上传队列），报错信息用中文列名并解释后果；同时新增 `REQUIRED_LABELS`（所有必填列都报中文名而不是内部字段名）。列在但单元格为空的行仍按 USD 计算，改为单独计数并告警（`blankCurrencyRows`）；出现 USD/THB 之外的币种也告警（`EXPECTED_CURRENCIES`）。④**预置汇率**——新增迁移 `20260909160000_seed_thb_exchange_rate.sql`，`ON CONFLICT DO NOTHING` 写入 USD=1、THB=32.5（语义：1 美元 = 32.5 泰铢；折美元 = 本币 / 32.5），已手工维护过的值不被覆盖。⑤**上传历史批量操作**——每行加多选框；「全选当前筛选（N）」按月份+站点筛选结果**跨页**全选（不是只选当前页）；「删除选中（N）」批量删；「全部删除（N）」跨页跨筛选清空全部批次（二次确认 + 需输入「全部删除」）。`delete` action 扩展为支持 `upload_ids: []`（按 100 一批）与 `all: true`，`ad_upload_rows` 是 ON DELETE CASCADE 会连行一起删。**待人工**：跑迁移 `20260909160000_seed_thb_exchange_rate.sql`；重新部署 `attribution-upload`、`attribution-run`、`attribution-feishu`。 | supabase/migrations/20260909160000_seed_thb_exchange_rate.sql、supabase/functions/attribution-upload/index.ts、supabase/functions/_shared/{attribution,attribution-report}.ts、src/components/attribution/{SiteMismatchTable,UploadView}.tsx、src/routes/gmv-attribution-admin.tsx、src/lib/{adExcel,attributionApi,version}.ts

- ✅ 2026-09-09 [claude] **GMV 归因第四轮（更正站点结论 + 复核 Excel 取数）**：①**更正上一轮的错误结论**——上一轮把「4 月归因大面积缺失」归因为「飞书建联表国家是手填中文、跟上传站点代码对不上」，经项目负责人核对**是错的**：建联表 C 列、剪辑表 D 列填的都是站点代码（PH / TH / VN / US / MX-AR），跟文件名同源、本来就能对上。站点放宽（`lookupIdentity` 全局兜底）仍按要求保留，但它兜的是「同一达人换站点」「建联表站点填错/留空」，不是原先说的那个原因；代码注释与 ARCHITECTURE 已改正。②**复核 Excel 取数逻辑，修掉一类静默错误**：`adExcel.ts` 原 `toNum` 只处理千分位，遇到币种符号（₱ $ ฿）、不换行空格、全角数字、括号负数一律 `Number()` 得 NaN 后**静默记 0**——整列取错时表面看不出任何异常。改成统一走新的 `parseNumeric`（NFKC 归一 + 去空格/不换行空格/窄空格 + 去币种符号百分号 + 括号负数），解析失败返回 NaN 由调用方决定，并**统计「原文非空但解析不出数字」的金额单元格数**。③**上传预览加诊断**：`ParsedFile.totals` 新增 `gmvRows`（GMV 非 0 的行数）、`byCurrency`（按行内「货币」列分组的原币种小计）；`ParsedFile.diagnostics` 暴露「缺货币列 / 解析失败单元格数+样本 / 未识别表头」。上传页 GMV 列改为**分币种展示**（如 `PHP 4,998,897`），新增「有GMV行数」列，并在表格下方对四种情况直接告警：缺货币列 / 有解析失败单元格 / 文件内多币种 / 全部行 GMV 为 0。**缺「货币」列是最危险的一种**——`ParsedRow.currency` 会默认 USD，`findMissingCurrencies` 因此不报错，非美元站点 GMV 会被当成美元放大几十倍，之前完全无提示。④**修 `non_usd` 误报**：`aggregateResults` 原来把「缺汇率未计入」和「已折算计入」的非美元币种混在同一个 `non_usd` 数组，前端一律显示成「⚠ 存在非 USD 数据未计入」，正常折算的数据也会被当成丢失。每项加 `usd_rate` / `gmv_usd`：`usd_rate=null` 才是真未计入（红色告警 + 指引去设置页补汇率），有值的单独渲染成折算明细（`PHP 4,998,897 ÷ 57 = $87,700`）供人工核对量级。（已重新部署） | src/lib/adExcel.ts、src/components/attribution/{UploadView,ProgressBoard}.tsx、src/lib/{attributionApi,version}.ts、supabase/functions/_shared/{attribution,attribution-report}.ts

- ✅ 2026-09-09 [claude] **GMV 归因第三轮（口径放宽 + 上传页修正）**：①**「上传广告表」列名修正**——「GMV」→「GMV（原币种）」、「商品卡行」→「商品卡行数」，上传历史的「GMV」→「GMV（USD）」，并在卡片说明里写清「行数/商品卡行数是 Excel 原始行计数，GMV（原币种）是总收入合计、还没折美元」。**注意**：这一列本来就是金额合计（`totals.gmv += gross_revenue`），不是 GMV 行数，所以按实际口径改名而不是改成「GMV行数」。②**去掉「Excel 上传」页底部冗余提示**——归因结果已单列成一个 tab，底部那段「已生成归因结果…请切换到归因结果标签」/「加载中…」多余，且加载失败时「加载中…」会一直挂着，整块删除。③**暂时关闭站点匹配**——昵称/用户名查表原本强制「站点 + 名字」双匹配（`identityKey`），但 `creator_ownership.country` 是飞书建联表 C 列 BD 手填原文（「美国」「菲律宾」…），跟上传文件名解析出的站点代码（US / PH…）对不上，导致整条昵称路径全部落空。新增 `lookupIdentity`（先精确、后全局兜底）+ `addGlobalFallback`（`loadAttrContext` 额外写 country='' 的全局键，同名跨站点冲突时按「昵称优先于用户名 → 最早建联日期 → BD 名」确定性取一条），`manualAlias`/`ownership`/`vidAlias` 三张表统一走它。④**暂时关闭 2000 美元阈值**——`KPI_MIN_SITE_USD` 2000→0，`counted_gmv` = 全部 GMV，前端阈值 ≤0 时不再显示阈值提示、不再置灰低于阈值的格子。⑤**4 月数据大面积缺失定位**：根因就是 ③，站点键对不上 → 昵称路径 100% 落空 → GMV 全部掉进「商品卡 + 无建联」两个桶，BD/剪辑表几乎全 0；另外发现「无建联达人」12 个月趋势表返回空是 `unmatched_trend` 把 12 个月 × 全部站点的批次逐个走 `fetchAllRows`（22 列 + 全部桶）拉全量、Edge Function 超时，改成新的 `fetchUnmatchedRows`（`attr_bucket='UNMATCHED'` 过滤下推到数据库、只取 3 列）。⑥**上传状态矩阵固定列**——`COUNTRY_ORDER`（PH/TH/VN/MY/SG/PHL/PH2/MX-AR/MX-NE/MX-SJ/US/JP）全部保留为固定列，即使一条记录都没有也有列，未上传的格子留空（不再显示灰色「无数据」标签），未知站点仍排在末尾。**已处理**：2026-09-09 已重新部署 `attribution-upload`、`attribution-run`、`attribution-feishu`、`attribution-sync-creators` 四个 Edge Function，`_shared/attribution.ts`、`_shared/attribution-report.ts` 新口径已同步打包生效。 | supabase/functions/attribution-upload/index.ts、supabase/functions/_shared/{attribution,attribution-report}.ts、src/components/attribution/{UploadView,UploadStatusMatrix,ProgressBoard,StaffCountryTable}.tsx、src/lib/version.ts

- ✅ 2026-09-09 [claude] **GMV 归因 V1 第二轮**：用户实测反馈修复+功能补齐。①**修复上传去重误伤分卷文件**：第一轮的重复检测按 (country,month) 拦截，导致「JP MAX 202508.1.xlsx」「JP MAX 202508.2.xlsx」这类正常分卷文件互相报重复、确认替换会把另一份删掉；改成按 (country,month,file_name) 精确匹配，只挡真正同名重复上传。②**修复大文件 finalize 报网络错误**：`persistRunArtifacts` 原来按 500 个一批用 `.in("alias_norm",...)` 查 MANUAL 别名，是 GET 请求，遇到大量中日文/emoji 昵称时 URL 被撑爆导致 `TypeError: error sending request`；改成一次性拉全表 MANUAL 别名（该表本身很小）在内存里比对。③**修复飞书判定读回从未生效的列错位 bug**：`read-judgments` 只读 A2:I（9列，到状态列为止），却在读 J列人工判定BD/K列备注（应为索引9/10，实际读到的是G/H首次/最近发现日期），导致飞书表回填的判定实际从未被正确读取；改成读 A2:L 并修正列索引。④**新增网页端直接判定**：「归因审查」面板不再只能看，改成每条审查项展示结构化冲突详情（VID双登记显示候选人+角色+登记日期；别名冲突显示各方 VID 证据数/示例；保护期抢注显示当前归属+各抢注方日期；交接边界显示样本），可直接在页面选择归属人或输入姓名+备注提交，新增 `attribution-feishu` 的 `submit-judgment` action：立即写数据库（`attribution_review` + 昵称类同时写 `creator_alias` MANUAL）+ 尽力同步回飞书表对应行（找不到该行则连同判定一起补一行），飞书同步失败不影响数据库判定已生效。⑤**上传历史体验**：分页 20 条/页；新增「上传状态矩阵」子 tab（行=月份新→旧、列=站点，格子=已归因/部分归因/上传中/失败/无数据），`list` action 上限从 100 提到 2000 以支撑矩阵看到全部历史。⑥**GMV 归因页（`/gmv-attribution`，管理者视角）重做**：不再用卡片+KPI阈值样式，BD/剪辑改成纯表格（姓名/角色/状态/GMV/按站点），过滤掉姓名为空或 GMV=0 的行；「无建联达人」新增「TOP（当月）/近12个月趋势表」两个子 tab，趋势表新增 `unmatched_trend` action 按 (国家,达人昵称) 聚合最近 12 个月 UNMATCHED 桶 GMV，可按站点筛选、按合计降序。`GMV 归因·管理` 页不受影响，仍是原有卡片+KPI阈值展示。**答复用户关于口径的疑问**：GMV 归因页面/上传历史表里的 GMV 已经是折美元后的口径（`aggregateResults`/`total_revenue` 都过了汇率转换）；唯一例外是「选择文件」待上传预览表（尚未提交、前端本地解析）显示的是原始币种金额，未折算。「行数」「商品卡行」都是 Excel 原始行计数（未做任何分组/去重），「商品卡行」= creative_type 归一化后等于 product_card 的行数。**待人工**：重新部署 `attribution-upload`、`attribution-feishu` 两个 Edge Function（本轮唯一改动的两个函数，无新迁移）。 | supabase/functions/{attribution-upload,attribution-feishu}/index.ts、supabase/functions/_shared/attribution-report.ts、src/routes/gmv-attribution.tsx、src/components/attribution/{UploadView,ReviewPanel,UploadStatusMatrix,UnmatchedTrendTable}.tsx、src/lib/{api,attributionApi,version}.ts

- ✅ 2026-09-09 [claude] **GMV 归因 V1 落地**（按 `GMV_ATTRIBUTION_V1_PLAN.md`）：①「GMV 归因」与「GMV 归因·管理」的「月度进度」都从读官方 API 链路（`attribution-run`/`gmv_attr_monthly_agg`/`gmv_max_vid_daily`，代码保留不删）切换为读 Excel 上传按月合并聚合（`attribution-upload` 的 `get action + merged:true`）；两页均为管理者视角展示全量口径（含离职、6 桶明细），2000 美元 KPI 阈值仅作展示提示不自动隐藏——按项目负责人确认，同事专属查看页留待后续单独加 tab，本次不做该权限/过滤分离。②修复同站点同月重复上传导致月度汇总翻倍：`attribution-upload` 的 `create` action 增加 `(country,month)` 状态为 UPLOADING/READY 的重复检测，默认报错（`payload.duplicate`），`replace_existing=true` 先删旧记录（级联删其行）再插入；`UploadView.tsx` 捕获后弹窗确认自动重试。③汇率：修正 `aggregateResults` 换算公式为「本币金额 / usd_rate」（`usd_rate` 语义统一为「1 美元 = 多少本币」，如 THB 填 33；此前是乘不是除，且历史未有非 USD 汇率数据，无存量脏数据需要迁移）；`finalize` 时新增 `findMissingCurrencies` 校验，缺币种直接报错（`payload.missing_currencies`）且不写 `attr_*`/不置 READY；`UploadView.tsx` 捕获后弹出输入框逐个补录（1 美元=多少本币）自动重试；设置页新增「GMV 归因汇率」tab（`ExchangeRateCard.tsx`，仅管理员可见）维护 `gmv_exchange_rates`。④新增唯一 VID 汇总导出：`attribution-upload` 新增 `export_vid_summary` action，按 (国家,VID,商品ID) 重新分组聚合（GMV/消耗折美元求和，ROI/CTR/CVR 聚合后重算，分母 0 留空；达人昵称取组内出现最多的），PID→SKU 复用 `gmv-max-query` 的 skuByPid 取首条模式；前端在「月度进度」toolbar 新增「导出唯一VID汇总」按钮，用现有 `xlsx` 库本地生成 14 列文件（未走服务端生成二进制，实现更简单、复用现有前端 xlsx 落地方式）。⑤`src/lib/api.ts` 的 `invokeFn` 增加把服务端返回的完整错误 JSON 挂到 `Error.payload`，供以上两处结构化错误（duplicate/missing_currencies）前端读取。**协作说明**：本任务涉及文件与 codex 认领的「GMV 归因进度（按审核计划重构）」（2026-07-11起，近两个月未推进）完全重叠，按项目负责人确认直接接手执行并已在任务板标注取代关系。**待人工**：重新部署 `attribution-upload` Edge Function（唯一改动的函数，未新建/未改其它函数，无需跑迁移）。 | supabase/functions/attribution-upload/index.ts、supabase/functions/_shared/attribution-report.ts、src/routes/{gmv-attribution,gmv-attribution-admin}.tsx、src/components/attribution/UploadView.tsx、src/components/settings/ExchangeRateCard.tsx、src/routes/settings.tsx、src/lib/{api,attributionApi,version}.ts

- ✅ 2026-08-26 [claude] 新增只读诊断 Edge Function `gmv-max-identity-get`：透传 TikTok `GET /gmv_max/identity/get/`，用于在 API测试页验证「授权所有账号（BC 已授权 TikTok 账号）」能否被查到，为「GMV Max 建单缺 identity_list」修复计划（见本文件下方任务）做前期验证，本身只读不建单不改动任何数据。已于 2026-08-27 由 lovable 重新部署。 | supabase/functions/gmv-max-identity-get/index.ts, src/lib/version.ts | 部署后在「API测试」页「通用查询指令」框粘贴：`gmv-max-identity-get {"advertiser_id":"你的广告户ID"}` 执行测试，store_id 会自动从 advertiser_countries.shop_id 取，不用手填。

- ✅ 2026-08-26 [claude] GMV MAX新建页面底部加两个 TikTok 官方 API 文档链接（新标签页打开）：「GMV MAX创建API」→ https://business-api.tiktok.com/portal/docs/create-product-gmv-max-campaigns/v1.3 ，「GMV MAX修改API」→ https://business-api.tiktok.com/portal/docs/update-a-gmv-max-campaign/v1.3 。纯前端文案改动。 | src/routes/gmv-max-create.tsx, src/lib/version.ts | 不需要做任何事，Lovable 同步 main 自动生效。

- ✅ 2026-08-26 [claude] GMV MAX新建页面 · 恢复上下排版：「单个新建」/「Excel 批量新建」两张卡片改回上下堆叠（撤销上一次的左右并排 grid）；「单个新建」内部改为第一行 6 项并排（广告户/广告组名称/ROI/预算/开始时间/结束时间，`grid-cols-2 sm:grid-cols-3 lg:grid-cols-6`），第二行商品ID多行文本框独占整行（`rows={2}` 默认两行高、`w-full`）。仅布局调整，未改校验/提交逻辑。 | src/routes/gmv-max-create.tsx, src/lib/version.ts | 不需要做任何事，Lovable 同步 main 自动生效。

- ✅ 2026-08-26 [claude] GMV MAX新建页面 · 「单个新建」/「Excel 批量新建」两张卡片改左右并排（`grid lg:grid-cols-2 items-stretch`，两卡整体高度保持一致）；单个新建内部布局改回：广告户/广告组名称同一行，下方商品ID文本框与 ROI/预算/开始时间/结束时间 4 项左右并排（flex + `items-stretch` + 商品ID 用 `flex-1` 自适应高度，下边缘自动跟结束时间框对齐，不用硬编码像素）；商品ID提示语「（一行一个，也支持用逗号分隔，中英文逗号均可，框内自动换行）」拆到独立第二行显示，不与「商品ID」共一行挤压换行。仅布局调整，未改校验/提交逻辑。 | src/routes/gmv-max-create.tsx, src/lib/version.ts | 不需要做任何事，Lovable 同步 main 自动生效。

- ✅ 2026-08-26 [claude] GMV MAX新建页面 · Excel 批量新建交互优化：①「下载模板」右侧改为「上传文件」按钮（隐藏 input + 触发点击，样式同「下载模板」），不再用原生文件选择框；②上传后直接在模板正下方渲染预览表格（含每行商品ID/ROI/预算/时间等全部字段），不用等点「批量创建」才能看到内容；③点「执行创建」后改为逐行串行调用 `gmv-max-adgroup-batch-create`（每次只传 1 行），每行状态在表格里实时刷新为「创建中…→成功/失败」，校验不通过的行直接标「校验未通过」并跳过调用，不再要求整批先修完校验错误才能提交；④结果表格支持「下载结果」导出为 xlsx（含状态列 + CampaignID/错误原因列）；⑤下载模板生成逻辑改为对 A:C（广告户ID/广告组名称/商品ID）列预设文本格式（`z:"@"`，避免大数字 ID 被 Excel 转科学计数法），F:G（开始/结束时间）列预设 `yyyy-mm-dd hh:mm:ss` 日期时间格式，预格式化到 200 行（对齐批量接口单次上限），后续在模板里继续填行也保持格式。纯前端改动，未碰 Edge Function / DB。 | src/routes/gmv-max-create.tsx, src/lib/version.ts | 不需要做任何事，Lovable 同步 main 自动生效。

- ✅ 2026-08-13 [claude] 广告户启用/停用开关 + 国家唯一性收紧为仅在启用广告户间强制；执行授权目标改为「素材列表筛选」与「待授权账户面板开关」AND 生效；执行授权拉取只读 BD 角色；人员表加只读「飞书表格」派生列。**待人工**：跑迁移 `20260813120000_advertiser_countries_active.sql`。`tiktok-connections`、`feishu-read`、`authorize-batch` 三个 Edge Function 已重新部署。 | supabase/migrations/20260813120000_advertiser_countries_active.sql, supabase/functions/{tiktok-connections,feishu-read,authorize-batch}/index.ts, src/lib/store.ts, src/components/settings/{AccountsTable,StaffTable}.tsx, src/routes/index.tsx

- ✅ 2026-08-13 [lovable] 修复 TikTok 授权回调地址随预览域名变化的问题：前端和 `tiktok-oauth-init` 云函数均固定使用已登记的正式回调 `https://ads-auth.lovable.app/oauth/tiktok/callback`，旧前端传入的预览地址也不会被采用。

- ✅ 2026-07-13 [codex] GMV Max 日报移除 VID 查找，仅保留日期/国家聚合；「数据行数」改为「素材数（去重 VID）」。

- ✅ 2026-07-09 [claude] 修复自动授权静默失败（成功0/失败0/无授权账号0）：`authorize-batch` 补 `x-cron-key` 放行（此前 cron 调用被 401 拒绝）；`authorize-log` RPC 参数名对齐 `_key`；corsHeaders 加 `x-cron-key`；cron 飞书通知附带错误摘要。**待人工**：重新部署 `authorize-batch`、`authorize-log` 两个 Edge Function（authorize-cron 路由随 Lovable 前端自动部署）

- ✅ 2026-07-06 [codex] 修复 `gmv-max-live-status`：移除当前报表维度不支持的 `tt_account_name`、`tt_account_authorization_type`、`shop_content_type`

- ✅ 2026-07-06 [codex] 新增 `gmv-max-live-status`，按广告户、Campaign、商品和 VID 直接查询 TikTok BC；抽取共享 `ttGet` 限速重试客户端，不读取或写入 GMV 明细表

- ✅ 2026-06-10 [claude] 建立协同文档体系
- ✅ 2026-06-10 [lovable] feishu-data 默认 tab=gmv、日期=昨天（已合入）
- ✅ 2026-06-10 [lovable] pg_cron 两条 GMV Max job 写入（gmv-max-sync-yesterday / today-hourly）
- ✅ 2026-06-10 [lovable] gmv_max_vid_daily 加 4 条索引（country+stat_date / advertiser_id+stat_date / vid / stat_date BRIN）
- ✅ 2026-06-10 [lovable] 每日 08:00 自动授权 cron：新 authorize-cron 路由 + authorize_cron_state 表 + 飞书机器人通知 + 3 个 Edge Function 加 x-cron-key bypass + pg_cron job authorize-daily-0800
- ✅ 2026-06-10 [lovable] feishu-read 固定列布局 + 执行授权页可折叠使用说明
- ✅ 2026-06-26 [codex] 新增飞书表接入指南文档，说明新项目读表、回写、鉴权、secrets 和常见错误
- ✅ 2026-07-05 [claude] 适配授权码表新列布局（sheet 改名建联-姓名；Q=VID / R=授权码 / J=SKU；回写改 V/W 列；staff_sheets 改名迁移）。**待人工**：① 在 5 张建联表 U 列后加 V=投放日期、W=回写状态表头 ② 部署 3 个 Edge Function + 跑迁移 ③ 小范围验证读取/回写
